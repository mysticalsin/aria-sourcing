---
project: MSourcing / ARIA
shift: 43
agent: codex-gpt-5
updated: 2026-07-16 09:58 EDT
status: migration-0035-implemented-local-db-proof-blocked
---

# Handoff - migration 0035 local implementation done, DB proof blocked by Docker socket

## Current state

- Working tree contains the local implementation for approved `.rocket-fuel/PLAN.md` rev 4, sections A/B/C:
  - `supabase/migrations/0035_candidate_corpus_mirror.sql`
  - `tests/candidates-corpus-db.sh`
  - `package.json`
  - `tests/test-manifest.mjs`
  - `tests/test-manifest-contract.mts`
  - `docs/TESTING.md`
  - `.github/workflows/ci.yml`
- `request_candidate_erasure`, the receipt `store_name` enum, `tests/db/candidate-erasure-authority.sql`, and `src/` were not modified.
- The prior relay baton was archived to `_relay/archive/2026-07-16-0958-codex-gpt-5.md`.
- Graphify navigation was probed first and failed with `zsh:1: no such file or directory: /graphify`; `graphify-out/wiki/index.md` is absent, so raw-file fallback was used.

## Done this shift

- Added `public.candidates` with the approved composite key `(workspace_id, campaign_id, id)`, indexes, forced RLS, direct grants revoked from `public`, `anon`, `authenticated`, `service_role`, and `authenticator`, and owner access policy for `postgres, supabase_admin`.
- Added `public.sync_candidates_corpus()` as a best-effort `SECURITY DEFINER` trigger function:
  - `search_path = pg_catalog, public, pg_temp`
  - absent or non-array `new.state->'candidates'` returns `null`
  - body is wrapped in `exception when others then raise warning ...; return null`
  - upserts live JSON candidates into the shadow table
  - skips tombstoned candidate elements using `candidate_erasure_identifier_hmac(new.workspace_id, 'candidate_id', elem.value->>'id')` against `candidate_erasure_suppression_tombstones`
  - deletes absent rows scoped to the workspace live-set after the array guard
- Added split mirror triggers:
  - `candidates_corpus_mirror_ins` after insert, no `WHEN`
  - `candidates_corpus_mirror_upd` after update of state, `WHEN old.state->'candidates' is distinct from new.state->'candidates'`
- Added `public.cleanup_erased_candidate_mirror()` and fail-closed `candidate_erasure_requests_mirror_cleanup` trigger:
  - fires after insert or update on `candidate_erasure_requests`
  - `WHEN new.status <> 'blocked_legal_hold'`
  - deletes by `(workspace_id, campaign_id, candidate_id)`
  - not wrapped, so cleanup failure aborts erasure
- Added `tests/candidates-corpus-db.sh`, cloned from the disposable Postgres harness style. It applies migrations, reapplies 0035, and asserts:
  - insert-sync
  - populate
  - remove-one
  - idempotent
  - unchanged-skip
  - erase manual_required and completed paths
  - re-materialization-blocked
  - RLS
  - malformed no-op
  - expected pass line: `9 scenarios, 10 assertions, 0 failed`
- Registered the new DB gate:
  - `package.json`: `test:db-candidates-corpus`
  - `tests/test-manifest.mjs`: command `candidates-corpus-db`, database group
  - `tests/test-manifest-contract.mts`: database group recomputed to count `13`, digest `390da52a9dc0a18c01ba9f66095201e1c5dffcd9cce9987e1e748d7c315434b1`
  - `docs/TESTING.md`
  - `.github/workflows/ci.yml`

## Verification

- Passed: `npm run typecheck`
- Passed: `npm run typecheck:tests`
- Passed via Node loader fallback: `node --import tsx tests/docs-truth.mts`, output `RESULT docs-truth: 44 passed, 0 failed`
- Passed via Node loader fallback: `node --import tsx --test tests/test-manifest-contract.mts`, output `8` tests, `7` pass, `0` fail, `1` skipped npm-lifecycle trace`
- Passed: `bash -n tests/candidates-corpus-db.sh`
- Passed: `git diff --check`
- Blocked: exact proof chain
  `npm run typecheck && npm run typecheck:tests && npm run test:db-candidate-erasure && bash tests/candidates-corpus-db.sh && npx tsx tests/docs-truth.mts`
  - Typecheck and test typecheck completed with exit 0.
  - Chain stopped at `npm run test:db-candidate-erasure`.
  - Exact error: `permission denied while trying to connect to the docker API at unix:///Users/tony/.colima/default/docker.sock`
- Blocked: `npm run test:db-candidates-corpus`
  - Exact error: `permission denied while trying to connect to the docker API at unix:///Users/tony/.colima/default/docker.sock`
- Blocked: exact `npx tsx tests/docs-truth.mts`
  - Exact error: `listen EPERM: operation not permitted .../tsx-501/<pid>.pipe`
  - Same error occurs with `TMPDIR=/private/tmp`.
  - `node --import tsx ...` works and was used only as fallback evidence.

## Blockers

- Docker socket access is denied for both Colima and Docker Desktop sockets from this sandbox:
  - Colima: `/Users/tony/.colima/default/docker.sock`
  - Docker Desktop: `/Users/tony/.docker/run/docker.sock`
- No local `postgres`, `initdb`, or `psql` binary is available, so the disposable database tests cannot be run without Docker access.
- The TSX CLI path cannot bind its IPC pipe in this sandbox. This blocks exact `npx tsx ...` commands, although `node --import tsx ...` works.

## Next steps

1. Run the exact proof chain in a shell with Docker socket access:
   `npm run typecheck && npm run typecheck:tests && npm run test:db-candidate-erasure && bash tests/candidates-corpus-db.sh && npx tsx tests/docs-truth.mts`
2. If the new DB test fails, inspect Postgres output first. Do not weaken or delete any of the nine scenario assertions.
3. If exact `npx tsx tests/docs-truth.mts` still fails outside this sandbox, compare with `node --import tsx tests/docs-truth.mts` and inspect local TSX IPC permissions.
4. After proof passes, commit only the seven implementation/registration files plus this relay archive and handoff update.

## Decisions made (don't relitigate)

- 0035 does not modify `request_candidate_erasure`.
- 0035 does not modify the receipt `store_name` enum.
- 0035 does not modify `tests/db/candidate-erasure-authority.sql`.
- 0035 does not touch product TypeScript under `src/`.
- Mirror sync is best-effort and must not abort workspace saves.
- Erasure cleanup is fail-closed and separate from mirror sync.
- Tombstone skipping is based on authoritative HMAC tombstones with `identifier_kind = 'candidate_id'`.

## Watch out

- Existing unrelated dirty/untracked files were present before this shift, including `_agent_state/claude-code/memory.json`, `.claude/`, `.gitlab-ci.yml`, several `_relay/2026-07-*` files, and `production-readiness/e2e-tony-2026-07-16/`. They were not reverted or cleaned.
- `git diff --check` does not cover untracked files in all Git configurations, but `bash -n tests/candidates-corpus-db.sh` passed for the new shell harness.
