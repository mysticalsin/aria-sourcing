---
project: MSourcing / ARIA
shift: 201
agent: cursor-cloud
updated: 2026-08-27T22:01Z
status: live-fly-at-2ffc428-demo-ux-plus-0068
---

# Handoff — Shift 201

## Current state

- **Branch:** `cursor/enterprise-autopilot-b91d` tip **`2ffc428`** (`2ffc428c2627592807808abd59ed52305305e544`)
- **Live Fly `aria-mantu-app`:** build **`2ffc428c2627592807808abd59ed52305305e544`** — `/api/ready` → `ok:true`, `status:ready`, migration **`0068_apply_workspace_patch_digest_path.sql`**
- Live ≠ `635eb4e` and ≠ `ba88302` (confirmed)
- Deploy path: official `print-fly-deploy-confirm.sh` → `/tmp/owner-deploy-confirm.env` → `fly-deploy-now.sh` (bootstrap push OK; `machine run` CLI false-timeout after migrations exit 0; step 3/3 secrets+app deploy completed manually with same identity)
- Bootstrap applied **0068** on prod DB (logs: `applying 0068_…` then `[migrate] complete`, exit_code=0)
- Tip lineage includes demo UX (`4ff518b` / merge `88b89b5`) + 0068 (`bba23f6`)
- Login page HTTP 200; password grant `twalteur@amaris.com` OK (tokens not logged)
- Microsoft path **SKIPPED** (owner); no outreach send

## Done this shift

1. Owner-ordered Fly deploy of tip `2ffc428` with mint confirm only
2. Bootstrap image pushed; migrations through **0068** applied
3. App image deployed; `/api/ready` matches tip + 0068
4. Proof: health ok; login 200; password grant ok

## Blockers

- None for this deploy. Optional: `/tmp/owner-llm.env` → `bash scripts/fly-apply-owner-llm-secrets.sh`
- MS Graph / live Outlook E2E still skipped

## Next steps

1. Re-verify demo UX live: Java brief title+openings=3; Outreach Dry-run without mailbox; hard reload `/` under ~2s
2. Confirm `apply_workspace_patch` no longer 404/PG `42883` (0068 live)
3. Optional owner LLM secrets apply
4. Do not Approve/send; do not invent secrets/confirm; do not resume Microsoft

## Decisions made (don't relitigate)

- Owner closed #32 — accept; push tip without reopen
- Owner skip Microsoft — still in force
- Force Dry-run when no real mailbox account
- Demo UX + 0068 co-located on `enterprise-autopilot-b91d`
- Local gate = CI authority; never invent deploy confirm
- VSS plain text/HTML production baseline; PDF OCR deferred
- `flyctl machine run` may false-timeout after fast `--rm` migrate success — trust machine logs/exit_code=0, then continue step 3/3

## Watch out

- After deploy, start loop machine if suspended (loop machine reached started this deploy)
- Do not set `ARIA_ALLOW_SKIP_LIVE_CALENDAR=1`
- Confirm must match tip SHA via `print-fly-deploy-confirm.sh` only
