---
project: MSourcing / ARIA
shift: 268
agent: cursor-cloud
updated: 2026-08-28T10:30Z
status: m365-blocked-audit-handoff-repair
---

# Handoff — Shift 268

## Current state

- **Live Fly tip:** `344fcaf` · migration **0071** · ready ok
- **Branch tip:** `1893bd2` (relay-only ahead of live — remint after owner secrets if code changes)
- **PR #35** (supersedes closed #29–#33): https://github.com/mysticalsin/aria-sourcing/pull/35
- **Gate:** `npx tsc --noEmit && npm test` · audit matrix target **59/59**
- **PARTIAL E2E on live:** 48 pass / 0 fail / 1 warn — expect **step 3c PASS** (live provenance); only **6b** skipped
- **M365 secrets missing (7)** · noperm · no `/tmp/owner-microsoft.env`
- **GHA/Vercel:** budget/rate-limit noise — **ignore** (Fly-only production)

## Done this shift

1. Audit matrix regression triage — HANDOFF missing golive/E2E tracking strings (repair in progress)

## Blockers

1. **Owner** Entra app in Mantu Sandbox `ce57ebe3…` + 7 Fly secrets (`_relay/M365-OWNER-UNBLOCK.md`)

## Next steps

1. Owner: `bash scripts/print-m365-owner-portal-checklist.sh` → portal app → apply
2. `bash scripts/print-fly-deploy-confirm.sh` → refresh confirm → remint if tip code changes
3. `bash scripts/print-fly-golive-status.sh` — tip vs live vs confirm (no secrets printed)
4. Connect Outlook (live) → Enable Graph webhook
5. `bash scripts/verify-m365-ready.sh` → strict PASS (incl. 6b)
6. `bash scripts/run-enterprise-e2e-partial.sh` or strict `e2e-workflow-test.sh` without partial flag after M365

## Decisions made (don't relitigate)

- **Production = Fly only** — never pretends full enterprise PASS while 6b skipped
- Entra tenant = **ce57ebe3…** (Mantu Group Sandbox)
- Graph OAuth tenant authority + OnlineMeetings.ReadWrite; callback does not invent scopes

## Production gate (Fly)

```bash
bash scripts/print-fly-golive-status.sh
bash scripts/print-fly-deploy-confirm.sh
curl -fsS https://aria-mantu-app.fly.dev/api/ready | jq '{ok,build,migration}'
ARIA_ALLOW_PARTIAL_M365_E2E=1 bash e2e-workflow-test.sh
# step 3c should show PASS when live build matches tip; MS-gap PARTIAL only when FAILS=0
bash scripts/post-m365-secrets-golive.sh
bash scripts/verify-m365-ready.sh
```
