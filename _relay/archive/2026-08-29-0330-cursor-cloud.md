---
project: MSourcing / ARIA
shift: 332
agent: cursor-cloud
updated: 2026-08-29T03:00Z
status: tip-ahead-app-e2e-no-dead-kimi-pin
---

# Handoff — Shift 332

## Current state

- **Branch / PR:** `cursor/enterprise-autopilot-b91d` · **PR #36** draft
- **Live Fly (app):** `46a1531` / **0073** (critics Hermes-first)
- **Tip:** e2e clears stale `AGENT_PROVIDER=kimi` when llm_auth=dead → `hermes` (needs tip deploy only if we treat e2e as image path — e2e runs from agent workspace, so tip_ahead_docs-ish; script is image-affecting per golive regex — tip_ahead_app)
- **PARTIAL E2E:** **62/0** last; critics still need owner LLM (Hermes upstream=dead Kimi)
- **Microsoft:** **DEFERRED** · **LLM:** `llm_auth=dead` · no `/tmp/owner-llm.env`

## Done this shift

1. E2E: re-probe Fly LLM; clear auth-dead shell `AGENT_PROVIDER` pins; fall back to `provider=hermes`
2. Confirmed only `KIMI_API_KEY` on Fly (dead); no alternate cloud keys

## Blockers

- Owner remint `/tmp/owner-llm.env` for live critics + cloud path
- Owner reopen Microsoft for Teams/Outlook book

## Next steps

```bash
# Microsoft path OFF unless owner re-enables.
unset AGENT_PROVIDER AGENT_MODEL
bash scripts/print-fly-golive-status.sh
bash scripts/print-fly-deploy-confirm.sh   # remint when tip_ahead_app
ls /tmp/owner-llm.env || true
ARIA_ALLOW_PARTIAL_M365_E2E=1 ARIA_ALLOW_PARTIAL_LLM_E2E=1 bash e2e-workflow-test.sh
# expect step 3c PASS with provenance=live; provenance / live=0 is quota
# expect Agent provider: hermes when llm_auth=dead (not kimi)
```

## Production gate (Fly)

```bash
bash scripts/print-fly-golive-status.sh
curl -fsS https://aria-mantu-app.fly.dev/api/ready | jq '{ok,build,migration}'
# step 3c should show PASS when running PARTIAL E2E; provenance / live=0 is quota
# Do NOT run verify-m365-ready / strict M365 E2E while Microsoft is deferred.
ARIA_ALLOW_PARTIAL_M365_E2E=1 ARIA_ALLOW_PARTIAL_LLM_E2E=1 bash e2e-workflow-test.sh
```

## Decisions made (don't relitigate)

- Production = Fly only; ignore Vercel/GHA empty-steps
- PR #36 only (supersedes #29)
- **2026-08-29: Owner — don’t do the Microsoft part**
- Deploy confirm remint is agent-owned (non-secret)
- Never pin auth-dead cloud AGENT_PROVIDER on Fly E2E — use live probe or hermes
- Workspace critics share Hermes-first stack; Hermes upstream still needs live key

## Watch out

- HANDOFF must keep “expect step 3c PASS” / “step 3c should show”
- Do not re-arm `m365-secrets-reprobe` unless owner asks
- Shell may export stale AGENT_PROVIDER=kimi — E2E must clear it
