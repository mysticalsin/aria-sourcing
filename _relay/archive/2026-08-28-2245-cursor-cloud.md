---
project: MSourcing / ARIA
shift: 308
agent: cursor-cloud
updated: 2026-08-28T22:25Z
status: honesty-batch-2-mailbox-mode-booking-kpi
---

# Handoff — Shift 308

## Current state

- **Branch / PR:** `cursor/enterprise-autopilot-b91d` · **PR #36** draft
- **Tip:** pending this commit · Live Fly still `fc8b54a` / **0071**
- **Audit:** **64/64** · **Gate:** green
- **M365:** `fly_m365_missing=7` · watcher armed · `m365-secrets-reprobe` timer (~30m)
- **LLM:** `llm_auth=dead`
- **Goal:** strict E2E PASS blocked on owner secrets

## Done this shift

1. Shift 307: mailbox send gate, critics toast, Approved awaiting-send, bookingNeedsCalendar KPIs, calendar skipped retryable
2. All mailbox integrations need `mode=live` (SMTP/API paste → degraded/mock, no Live unlock)
3. Calendar Ready + Completed→Interviewed gated on `bookingNeedsCalendar`
4. Floor + pendingApprovals include live Approved awaiting send
5. Geography/skills Contacted = `isRealSendFact` only
6. Outreach copy: LinkedIn alone never unlocks Live

## Blockers

Owner: 7 M365 + live LLM remint + deploy confirm → Connect Outlook → Graph webhook → strict E2E

## Next steps

```bash
bash scripts/print-fly-golive-status.sh
bash scripts/probe-m365-unblock.sh --apply
bash scripts/fly-apply-owner-llm-secrets.sh
bash scripts/probe-fly-llm-auth.sh
bash scripts/print-fly-deploy-confirm.sh
bash scripts/fly-enterprise-golive-when-ready.sh
bash scripts/verify-m365-ready.sh
env -u ARIA_ALLOW_PARTIAL_M365_E2E -u ARIA_ALLOW_PARTIAL_LLM_E2E bash e2e-workflow-test.sh
# expect step 3c PASS with provenance=live; strict RESULT: PASS
```

## Production gate (Fly)

```bash
bash scripts/print-fly-golive-status.sh
curl -fsS https://aria-mantu-app.fly.dev/api/ready | jq '{ok,build,migration}'
# step 3c should show PASS when running PARTIAL E2E; provenance / live=0 is quota
ARIA_ALLOW_PARTIAL_M365_E2E=1 ARIA_ALLOW_PARTIAL_LLM_E2E=1 bash e2e-workflow-test.sh
```

## Decisions made (don't relitigate)

- Production = Fly only; ignore Vercel/GHA
- PR #36 only; goal until strict Fly PASS
- Calendar `skipped` = retryable; Completed→Interviewed needs teamsLink/calLink

## Watch out

- HANDOFF must keep “expect step 3c PASS” / “step 3c should show”
