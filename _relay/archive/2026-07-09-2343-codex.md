---
project: MSourcing / ARIA
shift: 5
agent: codex
updated: 2026-07-09 23:36
status: rock-3-built-verified
---

# Handoff — ARIA Rock 3 integration honesty

## Current state
- Rock 3 code is implemented in the working tree, uncommitted.
- Every `defaultIntegrations()` card with `real: false` now seeds as `status: "not_configured"` and `lastSync: null`.
- `testConnection()` now fails closed for `real:false` roadmap placeholders instead of returning `ok:true` for mock adapters.
- Stored client state now migrates to `STATE_VERSION = 16`; existing `real:false` integration cards are normalized to `not_configured` with `lastSync:null`, while `real:true` cards keep their usage history.
- `dispatchDue()` now includes `stats.unconfigured`; provider dry-runs are counted there instead of `stats.failed`.
- WhatsApp dry-run outcomes still do not send anything, release the ledger as `skipped`, and transition the outbox to a non-sent terminal state.
- `tests/integrations-honesty.mts` exists and asserts no `real:false` card is connected, all `real:false` cards have null `lastSync`, a real GitHub card remains connected, and placeholder connection tests fail closed.

## Done this shift
- Updated `src/lib/integrations.ts` for honest placeholder status and `testConnection()` behavior.
- Updated `src/lib/seed.ts` and `src/lib/store.ts` migration logic for old persisted placeholder state.
- Updated `src/lib/dispatch-outbound.ts` with the `unconfigured` counter and dry-run handling.
- Updated `tests/dispatch-outbound.mts` expectations for dry-run provider outcomes.
- Added `tests/integrations-honesty.mts`.
- Archived the previous baton to `_relay/archive/2026-07-09-2336-codex.md`.

## Blockers
- No code blockers in this sandbox.
- Plain `npm test` still cannot run here because bare `tsx` opens an IPC socket and fails with `listen EPERM` under the sandbox before tests execute. The same suite was run with each bare `tsx` command rewritten to `node --import tsx`.

## Verification
- `graphify query "MSourcing ARIA integrations real false connected status dispatch-outbound dry-run provider stats"` -> failed because `graphify-out/graph.json` is absent; raw-file fallback used after confirming no wiki index.
- `node --import tsx tests/integrations-honesty.mts` -> `RESULT integrations-honesty: 6 passed, 0 failed`
- `node --import tsx tests/dispatch-outbound.mts` -> `RESULT dispatch-outbound: 79 passed, 0 failed`
- `npx tsc --noEmit` -> exit 0
- `npm test` -> blocked before suites by `Error: listen EPERM ... /T/tsx-501/...pipe`
- Sandbox-safe full suite equivalent (`pretest` + `test`, bare `tsx` rewritten to `node --import tsx`) -> every listed suite passed, ending with `RESULT isolated-build: 6 passed, 0 failed`

## Next steps
1. Visionary runs `npx tsx tests/integrations-honesty.mts` outside the sandbox, per Rock 3 proof contract.
2. Visionary may run plain `npm test` outside the sandbox to prove the exact package script path without the local IPC restriction.
3. Commit only Rock 3 files plus the baton/archive after reviewing unrelated dirty files already present in this worktree.

## Decisions made (don't relitigate)
- Do not mark roadmap placeholders as connected just because the UI can show a mock card.
- Preserve `real:true` integration cards and their existing statuses.
- Use `lastSync: null` as the codebase's null-equivalent for never-synced integrations.
- Keep provider dry-run separate from real delivery failure in dispatch stats.
- Do not add dependencies or database migrations for this rock.

## Watch out
- `src/lib/store.ts` already had unrelated GitHub-location edits before Rock 3; this shift only added the integration migration change there.
- The worktree still contains many unrelated dirty files and untracked assets from prior rocks. Do not revert or bundle them blindly.
- The settings UI still has a generic Configure modal for concept cards; Rock 3 only makes seeded/persisted status and connection-test reporting honest.
