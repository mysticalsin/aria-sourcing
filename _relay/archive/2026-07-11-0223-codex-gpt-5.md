---
project: MSourcing / ARIA
shift: 22
agent: codex-gpt-5
updated: 2026-07-11 02:10 EDT
status: automatic-release-contained-local-fail-closed-repair-under-review-production-degraded
---

# Handoff - enterprise audit and remediation baseline

## Current state

- Audited branch: `deploy/fly-github-actions`.
- Audited and remote SHA: `05cda612130d941f63512d503541c0410f1fc0fd`.
- The full enterprise goal remains `IN_PROGRESS`. No pending provider, Flowise, two-user, recovery, or owner-acceptance criterion was closed.
- Consolidated audit: `_relay/2026-07-11-enterprise-audit.md`.
- Replacement plan: `docs/superpowers/plans/2026-07-11-aria-enterprise-remediation.md`.
- Local gates at the audited SHA:
  - `npx tsc --noEmit && npm test`: exit 0.
  - `npm run lint`: exit 0.
  - `npm run build:isolated`: exit 0 with a Node-engine warning from `@dust-tt/client`.
  - `docker compose config --quiet`: exit 0.
  - `npm audit --audit-level=high`: exit 1, one high and three moderate findings.
- GitHub exact-SHA CI runs `29139277737` and `29139278802` fail the dependency audit. Secret scan is skipped after that step.
- GitHub deploy run `29139277754` is a false pass. Its log contains REST 503 and repeated Auth 503, then declares success from app liveness only.
- GitHub workflow `Deploy Aria Mantu (Fly)` is now `disabled_manually`. It cannot deploy on a branch push while the release pipeline is being replaced.
- The deploy branch is not protected. The GitHub `Production` environment now requires reviewer `mysticalsin` and accepts deployments only from `deploy/fly-github-actions`; administrators can still bypass and self-review is permitted. `Preview` remains unprotected.
- Live Fly state during audit:
  - `aria-mantu-db` sole machine is stopped and exits with code 1 when started; not OOM and not requested stop.
  - Both `aria-mantu-auth` machines are stopped; a recent start exits with code 1.
  - PostgREST and Kong machines remain started.
  - App `/api/health` and Kong `/healthz` return 200.
  - Auth, REST, and careers data paths return 503.
- The working tree contains a Senior Full-Stack fail-closed repair to `deploy-fly.sh`, a new `tests/deploy-contract.mts`, and `package.json` registration. Local contract, syntax, lint, and typecheck proof exists, but Cybersecurity Director, QA, full-project, workflow, and live verification are not complete.
- `docker/bootstrap/run.fly.sh` is still a separate concurrent edit. It waits for Postgres but swallows Auth-owner reconciliation failure and is not approved.

## Done this shift

- Ran the mandatory Graphify query first. It failed because the graph and wiki entrypoint are absent, then raw inspection followed the documented fallback.
- Reconciled live git, goal ledgers, accepted design, current plan, prior findings, tests, CI, PR, deploy logs, live endpoints, and Fly machine state.
- Ran parallel agent isolation, security/data, docs/operations, and prior-finding revalidation audits.
- Ran real Chromium checks against local and live login/careers at desktop and mobile with reduced-motion coverage.
- Verified the mapped-IPv6 SSRF literal bypass at runtime.
- Verified that workspace member control of Databricks settings combines with stored-secret resolution and forwarding.
- Verified AgentSpec owner isolation, stored-spec run binding, agent memory, and candidate conversation routing are incomplete.
- Verified repository size and large tracked artifacts.
- Proved from GitHub run `29139277754` that Fly classified the stopped database machine as good, after which the script printed `OK deploy db`, observed REST/Auth 503, and still reported the stack up.
- Disabled the unsafe push-triggered production workflow and verified its provider state as `disabled_manually`.
- Added and verified a Production-environment reviewer gate plus a custom policy restricting deployments to `deploy/fly-github-actions`.
- Updated the remediation plan so token containment, automatic-release freeze, minimum backup/restore proof, and release-blocking dependency/secret gates precede production recovery.
- Updated `_relay/codex-findings.md` with current open issues and corrected obsolete diagnoses.
- Archived shift 21 baton to `_relay/archive/2026-07-11-0047-claude-opus-4-8.md`.

## Blockers

- Production is not operational: database and Auth processes exit with code 1. Root-cause logs still need capture; do not guess from the exit code.
- Exact-SHA CI is red on the dependency audit.
- CodeRabbit 0.6.5 is signed out, so no CodeRabbit review ran.
- Production database ACL, migration, tenant, and restore checks cannot run until the database is recovered or a representative scratch environment is created.
- Real email, WhatsApp, two-user, Flowise, admin metrics, alert delivery, rollback, and target restore evidence are absent.
- ElevenLabs provider-side key rotation remains unproven.
- The exposed Fly deploy token remains active because the org token cannot create or revoke its replacement. Owner-level Fly access is required before further authenticated Fly work.
- The automatic deploy workflow is disabled and Production has a reviewer/branch gate, but the replacement workflow is still uncommitted and unverified in GitHub CI. Branch protection is not configured.
- The local fail-closed deploy repair does not fix database exit code 1, real readiness, mutable bootstrap `latest`, workflow supply-chain controls, or the unsafe bootstrap failure swallow.
- Audit and repair artifacts remain intentionally uncommitted and unpushed while review is active.

## Next steps

1. Complete Cybersecurity Director, QA, and independent validator review of the local fail-closed repair; close every P0/P1 test gap.
2. Rotate and revoke the exposed Fly token with owner-level access, update GitHub secrets without printing values, and review Fly activity.
3. Replace the disabled workflow with exact-SHA CI dependency, immutable action pins, protected environment, concurrency, temporary secret cleanup, and immutable migration image identity.
4. Preserve and scratch-restore the sole database volume, then capture startup logs, reproduce exit code 1 on a disposable volume, and fix the root cause without deleting production data.
5. Add real readiness and run the full local/container/CI contract before any controlled release.
6. Continue through security boundaries, agent authority, frontend truth/a11y, Flowise, operations, repository cleanup, and exact-SHA acceptance.

## Decisions made (don't relitigate)

- ARIA is the identity, policy, audit, and delivery control plane. Flowise is private authoring only.
- AgentSpec is the stable agent employee identity; runs, memory, conversations, sender binding, budgets, and audit must reference it.
- Replies route by provider context and canonical conversation, never latest outbound address or active campaign.
- Live backend failure is degraded state, never synthetic demo state.
- Human review remains the default release authority until a narrow auto-answer path has canary, kill switch, and acceptance proof.
- LinkedIn remains official-integration or assisted draft-only. No bypass work.
- Do not rewrite history or delete tracked evidence/binaries without a separate retention and migration decision.

## Watch out

- The deploy branch is 56 commits ahead of `origin/vercel-demo`; PR #3 contains 611 changed files because the base is stale.
- `origin/main`, local `main`, local `vercel-demo`, and `origin/vercel-demo` do not currently represent one canonical release line.
- Querying liveness alone reproduces the false-green state. Always check Auth, REST, database, migrations, and build identity.
- `.localbin/` contains committed macOS arm64 binaries. Do not remove them casually while setup scripts still reference them.
- The single production database volume has no proven replica or fresh target restore. Preserve it until backup proof exists.
