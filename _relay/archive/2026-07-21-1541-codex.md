---
project: MSourcing / ARIA
shift: 46
agent: codex
updated: 2026-07-21 15:07 EDT
status: five-rock-1-fixes-in-working-tree-proof-blocked-by-sandbox-and-docker
---

# Handoff — Shift 46

## Current state

- Working tree contains fixes for all five canonical Rock 1 defects from `_relay/2026-07-19-50k-enterprise-readiness-verification.md`.
- Local commits are BLOCKED by sandbox `.git` write permissions:
  `fatal: Unable to create '/Users/tony/Library/CloudStorage/OneDrive-MantuGroup/Documents/Chief of Staff/Apps Source/MSourcing/.git/index.lock': Operation not permitted`
- Exact full gate was attempted:
  `npm run typecheck && npm run typecheck:tests && npm run lint && npm run test:all && npm run test:database`
  - `npm run typecheck`: reached and exited 0.
  - `npm run typecheck:tests`: reached and exited 0.
  - `npm run lint`: no output for over eight minutes; sandbox denied `ps` inspection with `operation not permitted: ps`; run was interrupted with Ctrl-C, exit 130.
- Separate targeted static checks:
  - `npm run typecheck`: exit 0.
  - `npm run typecheck:tests`: exit 0.
  - `npx eslint scripts/run-test-manifest.mjs tests/test-manifest-contract.mts src/app/api/outreach/send/route.ts tests/dispatch-outbound.mts`: no output for over 90 seconds; interrupted, exit 130.
  - `git diff --check`: exit 0.
- Exact database proofs are BLOCKED by Docker socket permission:
  `permission denied while trying to connect to the docker API at unix:///Users/tony/.colima/default/docker.sock`
  This blocked `npm run test:database`, `bash tests/email-durability-db.sh`, and `bash tests/email-inbound-db.sh`.

## Done This Shift

- Read required sources:
  `.rocket-fuel/PLAN.md`, `.rocket-fuel/ROCKS.md`, `_relay/2026-07-19-50k-enterprise-readiness-verification.md`, `package.json`, `scripts/run-test-manifest.mjs`, `tests/test-manifest.mjs`, and the named failing tests/scripts.
- Attempted `/graphify query "MSourcing canonical test defects 2026-07-19 readiness verification"` first; shell returned `zsh:1: no such file or directory: /graphify`.
- Fixed stale recovery inventory:
  `docker/bootstrap/legacy-table-inventory.txt` now matches the sorted `expected_tables` invariant from `docker/bootstrap/legacy-baseline-invariants.sql`.
- Fixed stale SMS ordering source pin:
  `tests/dispatch-outbound.mts` now pins `performEmailSend(` as the post-guard provider primitive, not the old direct `sendViaProvider({` path.
- Fixed email send ambiguity:
  `src/app/api/outreach/send/route.ts` wraps the post-transport provider/reconciliation path; a thrown reconciliation update after transport now attempts `ambiguous` and returns `email-reconciliation-required` instead of escaping.
- Added immutable DB migration:
  `supabase/migrations/0047_email_delivery_events_service_role_select.sql` grants `SELECT` on `public.email_delivery_events` to `service_role`.
- Corrected inbound DB assertion shape:
  `tests/email-inbound-db.sh` explicitly casts the workspace-id equality probe to text. The RPC payload already returned JSON `ok: true`; the previous `true:t` came from PostgreSQL's implicit boolean display inside `concat_ws`.
- Fixed manifest runner portability:
  `scripts/run-test-manifest.mjs` runs manifest `tsx` commands as `node --import tsx ...` instead of spawning the tsx CLI IPC server. Contract updated in `tests/test-manifest-contract.mts`.

## Focused Proof

- `node --import tsx tests/recovery-schema-allowlists.mts`
  - `RESULT recovery-schema-allowlists: 14 passed, 0 failed`
- `node --import tsx tests/dispatch-outbound.mts`
  - `RESULT dispatch-outbound: 89 passed, 0 failed`
- `node --experimental-test-module-mocks --import tsx tests/email-send-ambiguity.mts`
  - `RESULT email-send-ambiguity: 71 passed, 0 failed`
- `node --import tsx tests/test-manifest-contract.mts`
  - TAP: 8 tests, 7 passed, 1 skipped, 0 failed.

## Blockers

- Cannot commit locally because `.git` is read-only to this sandbox profile.
- Cannot prove database suites because Docker socket access is denied.
- Cannot prove full `npm run lint` because ESLint hangs silently in this sandbox and `ps` inspection is denied.
- `node scripts/run-test-manifest.mjs --group pretest` now gets past tsx IPC but hits a sandbox socket policy in `tests/apollo-cleanup-worker.mts`:
  `listen EPERM: operation not permitted 127.0.0.1`

## Next Steps

1. In a normal local shell with `.git` write access, stage and commit the changed files in logical commits:
   - `docker/bootstrap/legacy-table-inventory.txt`
   - `tests/dispatch-outbound.mts`
   - `src/app/api/outreach/send/route.ts`
   - `supabase/migrations/0047_email_delivery_events_service_role_select.sql`
   - `tests/email-inbound-db.sh`
   - `scripts/run-test-manifest.mjs` and `tests/test-manifest-contract.mts`
2. Re-run exactly:
   `npm run typecheck && npm run typecheck:tests && npm run lint && npm run test:all && npm run test:database`
3. Re-run exactly:
   `bash tests/email-durability-db.sh`
4. Re-run exactly:
   `bash tests/email-inbound-db.sh`
5. If Docker still fails, report the socket error verbatim. Do not mark database proof done.

## Decisions Made (Don't Relitigate)

- No shipped numbered migration was edited.
- `workspace_state` normalization was not started.
- `sequences_enabled` was not changed.
- `.env*`, secrets, and production data were not touched.
- The inbound `true:t` assertion was a SQL probe-shape issue, not an RPC payload issue.
- The SMS ordering implementation is canonical: the API rejects SMS before database/provider side effects; the old `sendViaProvider({` string assertion was stale after the shared `performEmailSend` extraction.

## Watch Out

- The worktree had many pre-existing dirty and untracked files before this shift. Do not sweep them into commits.
- `_relay/HANDOFF.md` was already dirty before this rewrite; the previous baton is archived at `_relay/archive/2026-07-21-1507-codex.md`.
- `package.json` was dirty before this shift and was not modified for this fix set.
