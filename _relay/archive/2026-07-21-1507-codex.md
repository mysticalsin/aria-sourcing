---
project: MSourcing / ARIA
shift: 45
agent: claude-opus-4-8
updated: 2026-07-17 16:40 EDT
status: full-manifest-green-after-fixing-3-dead-ci-masked-defects (pushed fix/dead-ci-defects-2026-07-17 @ 6db10b0)
---

# Handoff — Shift 45 FINAL: full 208-cmd manifest green; 3 dead-CI-masked defects fixed + pushed

## Correction to the earlier shift-45 note below
The "Rock 2 REVISE → SHIP, all 8 gates green" section further down was an OVERCLAIM
— it verified the baton's 8-gate subset, not the full CI. On challenge, a complete
local run of the **208-command test manifest** showed the branch was NOT green: three
real, deterministic, dead-CI-masked defects (GitHub Actions is budget-dead, so nothing
had run the full suite in a while).

## The 3 real defects — fixed on branch `fix/dead-ci-defects-2026-07-17` (6db10b0), pushed to origin
1. `infra-release-contract`: asserted fly.app.toml `path="/api/ready"`; commit 082178e
   deliberately moved the proxy check to `/api/health`. Test updated.
2. scoring: `bc31d54` set requested-but-unknown dimension score to anchor 30; per Tony's
   decision unknown stays neutral 50 ("not provided"). Fixed `UNKNOWN_ANCHOR` 30→50 +
   rationale; `store-sourcing-actions` passes; `scoring-metrics` assertion updated to the
   neutral-50 invariant.
3. reviewed-schema baseline (sha256 + `expected_tables` + `expected_functions` +
   `legacy-table-inventory.txt`) predated shipped migrations 0034-0037 (calendar, corpus,
   person model). Regenerated all four mirrors per Tony's authorization. No schema change.

## Verification
Full 208-command manifest green on a local lane (GitHub clone of the branch + `npm ci`).
Env-only lane notes (pass on Linux CI): deploy-contract needs >20s on macOS; three DB
tests need a colima-shared `$HOME` mount; repository-hygiene needs `.git` present.

## Owner-side still open
- **GitHub Actions billing** — CI stays dark until raised; then CI can validate 6db10b0.
- Open a PR: https://github.com/mysticalsin/aria-sourcing-demo/compare/integration/sourcing-enrichment-on-main...fix/dead-ci-defects-2026-07-17
- Dependabot flags 1 moderate vuln on the default branch (GitHub push notice).
- Unpushed work on the frozen OneDrive tree (email-durability etc.) still secured at
  `~/msourcing-recovery-2026-07-17/`; recover once OneDrive hydrates.

---

## (Superseded, kept for history) Shift 45 first note: Rock 2 REVISE → SHIP (8-gate subset)

## Verdict: SHIP
Codex shift 44's REVISE was sandbox-only (tsx-CLI `listen EPERM` + colima-down), never a code
failure. Since then the work was committed (`8f2a4e4`) and pushed; the only real gap was green CI
proof, blocked by GitHub Actions budget exhaustion (owner-action 5b). This shift supplied it.

Proven head: **origin `839ff2e`** (= Rock 2 `8f2a4e4` + migration 0037 `3ff4852` + docs). Ran on a
fast-disk lane (shallow GitHub clone + `npm ci` from the pinned lockfile) because the OneDrive tree
was frozen-dehydrated and unbuildable. Byte-faithful to origin.

| Gate | Result |
|---|---|
| typecheck / typecheck:tests / lint | rc=0 clean |
| test:manifest (tsx CLI — codex socket blocker) | rc=0 real run |
| route-auth | 16 passed, 0 failed |
| docs-truth (tsx CLI — socket blocker) | 44 passed, 0 failed |
| db-candidates-corpus (migration 0036) | 20 assertions, 0 failed |
| db-candidate-erasure | pass |
| build:isolated | rc=0 all routes |

## Owner-side (not code)
- GitHub Actions budget → CI dead until billing raised; then re-run branch CI for `839ff2e`. Local run substitutes.
- OneDrive sync frozen → resume/pin `…/Apps Source/MSourcing` to make the live checkout usable.

## UNPUSHED WORK on this tree — secured, do not lose
Local HEAD `c95fe44` is unpushed and carries substantial uncommitted work NOT on origin: an
email-channel-durability feature (migration `0039`, `src/lib/email-send.ts`, `tests/email-durability-*`),
candidates-endpoint rate-limiting, `SUPABASE_COOKIE_OPTIONS` cookie hardening, and ~21 modified
API/worker/lib files. **Secured** as a snapshot + tar at `~/msourcing-recovery-2026-07-17/`
(28 code files, all captured). The unpushed COMMIT delta (`c95fe44` vs origin) lives only in cold
`.git` objects — capture it once OneDrive hydrates: `git log 839ff2e..c95fe44`, then reconcile/push.

## Prior — Shift 44 (codex-gpt-5, 2026-07-16 10:45 EDT): Rock 2 implemented, exact sandbox proof blocked

## Current state

- Implemented Phase 1 Rock 2 from approved `.rocket-fuel/PLAN.md` rev 3 in the local working tree.
- New files present:
  - `supabase/migrations/0036_candidate_corpus_read.sql`
  - `src/app/api/candidates/route.ts`
  - `src/lib/candidate-payload.ts`
  - `tests/candidates-route-auth.mts`
- Modified files for this rock:
  - `.env.local.example`
  - `src/lib/supabase/config.ts`
  - `src/app/candidates/page.tsx`
  - `tests/candidates-corpus-db.sh`
  - `tests/test-manifest.mjs`
  - `tests/test-manifest-contract.mts`
  - `_relay/HANDOFF.md`
  - `_relay/archive/2026-07-16-1045-codex-gpt-5.md`
- `supabase/migrations/0033_candidate_erasure_authority.sql` and `supabase/migrations/0035_candidate_corpus_mirror.sql` were not modified.
- `package.json`, `docs/TESTING.md`, and `.github/workflows/ci.yml` already had the existing `test:db-candidates-corpus` registration from 0035; no registration change was needed there for 0036.
- Graphify navigation was attempted first and failed because `/graphify` is not installed as a shell command and `graphify-out/wiki/index.md` is absent. Raw-file fallback was used.

## Done this shift

- Added migration 0036 with:
  - `public.mirror_workspace_candidates(uuid, jsonb)` shared core using the 0035 live-set logic.
  - `CREATE OR REPLACE public.sync_candidates_corpus()` delegating to the shared core while preserving the six pinned behaviors from 0035.
  - `public.backfill_candidates_corpus()` with per-workspace exception isolation and final notice counts.
  - `public.list_workspace_candidates(...)` as an authenticated-only SECURITY DEFINER read RPC with auth.uid guard, current workspace scoping, escaped ILIKE search, source/stage/campaign filters, match/recent sort, clamped limit/offset, and shared SQL spine for count and rows.
  - Explicit revokes for write-capable helpers from `public`, `anon`, `authenticated`, `service_role`, and `authenticator`, plus grant only to `authenticated` for the list RPC.
- Added `GET /api/candidates`:
  - `prodFailClosed` first, then live Supabase/session/user/role guard using `can(role, "view")`.
  - Apollo select no-store/nosniff response headers on every response.
  - Apollo-style 503 unavailable response for demo/no Supabase/no session.
  - zod query validation and cookie-bound RPC call.
  - Payload normalization through `candidateFromPayload()`, with malformed payloads dropped and only drop counts logged.
- Added `src/lib/candidate-payload.ts`:
  - Rejects missing id, campaignId, or name.
  - Builds a complete `Candidate` with typed defaults and full `ComplianceFlags` defaults.
  - Keeps `yearsExperience` as `null` when absent.
  - Derives avatar initials from name.
- Added flag-gated preview mode:
  - `NEXT_PUBLIC_ENABLE_CORPUS_SERVER_READ=false` in `.env.local.example`.
  - `corpusServerReadEnabled` in Supabase config.
  - `/candidates` uses existing store-backed path when the flag is off.
  - Flag-on preview fetches rows/total from `/api/candidates`, hides bulk selection, labels the count as server candidate count, and opens the drawer only when the server row exists locally.
- Extended `tests/candidates-corpus-db.sh`:
  - Applies migrations strictly once in numeric order, no 0035 reapply.
  - Adds backfill simulation and row-set idempotence check.
  - Adds helper ACL denial, tenant isolation, null auth, source filter, both sorts, pagination/total, and escaped wildcard search scenarios.
- Added and registered `tests/candidates-route-auth.mts`.
- Recomputed `tests/test-manifest-contract.mts` digests for the additional application test.
- Narrow patch after audit:
  - `src/app/api/candidates/route.ts` now returns `prodFailClosed()` directly with sensitive headers, instead of wrapping its response body.
  - `src/app/candidates/page.tsx` preview fetch uses a small runtime array guard instead of casting arbitrary JSON to `Candidate[]`.

## Verification

- Passed in exact chain before the TSX CLI block:
  - `npm run typecheck`
  - `npm run typecheck:tests`
  - `npm run lint`
- Blocked: `npm run test:manifest`
  - Exact error: `listen EPERM: operation not permitted /var/folders/5m/c_klcrrj4yj_jxhf4t6vhb080000gn/T/tsx-501/41552.pipe`
  - Retried with `TMPDIR=/private/tmp`; same `listen EPERM`.
  - A direct Node socket probe also failed: `EPERM listen EPERM: operation not permitted /private/tmp/codex-tsx-ipc-test-41957.sock`.
  - TCP listen probe also failed: `EPERM listen EPERM: operation not permitted 127.0.0.1`.
- Passed fallback for the same manifest contract:
  - `node --import tsx tests/test-manifest-contract.mts`
  - Output: 8 tests, 7 pass, 0 fail, 1 skipped npm-lifecycle trace.
- Passed exact focused route test:
  - `node --experimental-test-module-mocks --import tsx tests/candidates-route-auth.mts`
  - Output: `RESULT candidates-route-auth: 16 passed, 0 failed`.
- Blocked: exact `npx tsx tests/docs-truth.mts`
  - Exact error: `listen EPERM: operation not permitted .../tsx-501/46608.pipe`.
- Passed fallback docs truth:
  - `node --import tsx tests/docs-truth.mts`
  - Output: `RESULT docs-truth: 44 passed, 0 failed`.
- Passed: `bash -n tests/candidates-corpus-db.sh`.
- Passed: `git diff --check`.
- Blocked: `npm run test:db-candidates-corpus`
  - Exact error: `permission denied while trying to connect to the docker API at unix:///Users/tony/.colima/default/docker.sock`.
- Blocked: `npm run test:db-candidate-erasure`
  - Exact error: `permission denied while trying to connect to the docker API at unix:///Users/tony/.colima/default/docker.sock`.
- Deferred to Visionary Level 10 review, per Tony's instruction:
  - `tests/candidates-corpus-db.sh`
  - `npm run test:db-candidate-erasure`
  - `npm run build:isolated`

## Blockers

- This sandbox prohibits listening sockets. That blocks TSX CLI IPC and prevents exact `npm run test:manifest` and exact `npx tsx tests/docs-truth.mts` from completing, even though the same tests pass via `node --import tsx`.
- Docker socket access is denied at `unix:///Users/tony/.colima/default/docker.sock`, so both Docker DB gates are deferred.
- `build:isolated` was not run by instruction.
- Because Tony's requested exact sandbox proof list is not all green, the verdict is REVISE, not SHIP.

## Next steps

1. Run the exact sandbox proof in an environment that permits local sockets:
   `npm run typecheck && npm run typecheck:tests && npm run lint && npm run test:manifest && node --experimental-test-module-mocks --import tsx tests/candidates-route-auth.mts && npx tsx tests/docs-truth.mts`
2. Run the Visionary Level 10 deferred gates:
   `bash tests/candidates-corpus-db.sh`
   `npm run test:db-candidate-erasure`
   `npm run build:isolated`
3. If exact TSX CLI still fails outside this sandbox, decide whether to change lifecycle scripts to `node --import tsx ...` as a separate portability fix. Do not mix that with this rock unless Tony approves.
4. If Docker DB gates fail, inspect SQL output first. Do not weaken or delete any 0035 or 0036 assertions.

## Decisions made (don't relitigate)

- 0033 and 0035 remain untouched.
- Blob remains authoritative for writes.
- No dedupe, scoring, erasure, store write path, or campaign-tab cutover was changed.
- Flag default remains off.
- Route uses the cookie-bound authenticated client, not service role.
- The list RPC is authenticated-only; write-capable helpers remain postgres-only.
- Exact TSX CLI failure is a sandbox socket policy issue, evidenced by direct Node socket probes.

## Watch out

- Existing unrelated dirty/untracked files predated this work, including `_agent_state/claude-code/memory.json`, `.claude/`, `.gitlab-ci.yml`, `_relay/2026-07-*`, and `production-readiness/e2e-tony-2026-07-16/`. They were not reverted.
- `_agent_state/codex/memory.json` already had a local learning entry before this final baton; it remains dirty.
- `src/app/api/candidates/route.ts`, `src/lib/candidate-payload.ts`, `supabase/migrations/0036_candidate_corpus_read.sql`, and `tests/candidates-route-auth.mts` are untracked and must be added explicitly before any commit.
