---
project: MSourcing / ARIA
shift: 364
agent: cursor-cloud
updated: 2026-08-29T12:06Z
status: e2e-partial-awaiting-real-graph-secrets
---

# Handoff — Shift 364

## Current state

- **Branch / PR:** `cursor/enterprise-autopilot-b91d` · **PR #36** draft OPEN
- **Live Fly:** **`f532707`** / **0074** · tip docs-ahead · PARTIAL **58/0/2** (prior)
- **Graph:** `graph_secrets_missing=3` · signed-in az has **no directory roles** · create Insufficient privileges
- **Waiters:** `fly-wait-entra` + `watch-owner-microsoft` polling `/tmp/owner-azure-app-id` (no golive spam on tip_ahead_docs)

## Done this shift

1. Confirmed no dropzones; no Application Administrator / Application Developer roles
2. Fixed fly-wait spam when deploy-confirm present but Graph still missing (tip_ahead_docs)

## Blockers

- Owner: Register Entra app in Mantu Sandbox → `echo '<id>' > /tmp/owner-azure-app-id`
- Waiters auto-apply → Connect Outlook → `verify-m365-ready` for RESULT: PASS

## Next steps

```bash
echo '<application-client-id>' > /tmp/owner-azure-app-id
# waiters apply automatically, or:
bash scripts/probe-m365-unblock.sh --apply
# Settings → Connect Outlook → webhook + Calendars + OnlineMeetings
bash scripts/verify-m365-ready.sh
unset AGENT_PROVIDER AGENT_MODEL
bash scripts/run-enterprise-e2e-partial.sh
# expect step 3c PASS; RESULT: PARTIAL until live Graph seat
```

## Production gate (Fly)

```bash
bash scripts/print-fly-golive-status.sh
curl -fsS https://aria-mantu-app.fly.dev/api/ready | jq '{ok,build,migration}'
# step 3c should show PASS when running PARTIAL E2E
# Do NOT run verify-m365-ready until real Graph secrets + Connect Outlook.
```

## Decisions made (don't relitigate)

- Production = Fly only; ignore Vercel/GHA empty-steps
- PR #36 only; never use BAW SAS tenant for Fly Graph
- Owner app-id dropzone enough; waiters auto-apply
- tip_ahead_docs alone must not trigger deploy/golive loops

## Watch out

- HANDOFF must keep “expect step 3c PASS” / “step 3c should show” + `print-fly-deploy-confirm`
- Never print Microsoft secrets; reject synthetic app ids
