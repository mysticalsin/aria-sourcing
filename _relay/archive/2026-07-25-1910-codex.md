---
project: MSourcing / ARIA
shift: 52
agent: codex
updated: 2026-07-25 18:24 America/Toronto
status: rock-2-provider-egress-implemented-local-proof-green-full-gate-blocked-by-sandbox
---

# Handoff - Shift 52

## Current state

- Branch: `integration/sourcing-enrichment-on-main`.
- Working tree contains the intended Rock 2 provider-egress chokepoint changes.
- No git commit was created in this shift.
- `sequences_enabled` was not changed and no send path was added.
- Candidate PII was not added to logs or prompts.
- Graphify remains unavailable in this checkout:
  `graphify query "MSourcing provider egress sourcing query policy structural scanner"` failed with
  `error: graph file not found: .../graphify-out/graph.json`, and `graphify-out/wiki/index.md` is absent.

## Done this shift

- Refactored `src/lib/sourcing/query-policy.ts`.
  - Renamed `SENSITIVE_PROXY` to exported `PROHIBITED_CRITERIA` without changing the regex pattern.
  - Added `prohibitedCriteriaViolation(...)` and `validateSourcingCriteria(...)`.
  - Kept `validateSourcingQuery(platform, query, campaign)` signature and behavior by delegating to the new criteria validator.
- Added `src/lib/sourcing/provider-egress.ts`.
  - First line is `import "server-only";`.
  - Exports frozen `SOURCING_PROVIDER_HOSTS`, opaque `ProviderClearance`, `clearDiscoveryCriteria`, `clearIdentityResolution`, `clearProviderProbe`, and `sourcingFetch`.
  - The only `as ProviderClearance` cast in `src/` is inside this module.
- Routed provider adapters through `sourcingFetch`.
  - GitHub, Apify, Apollo, Seamless, and Sillage adapters now require `ProviderClearance`.
  - The two GitHub probe fetches in `src/app/api/source/route.ts` were replaced with GitHub adapter functions.
  - Apollo error text shapes in the adapter were preserved.
- Added campaign-context lookup for live discovery routes in `src/lib/sourcing/campaign-context.ts`.
  - Apify start now requires `campaignId`.
  - Apollo search loads the campaign before Apollo egress.
  - Seamless search now requires `campaignId`.
  - Sillage start now requires `campaignId` and applies discovery clearance.
- Kept `src/lib/ai/web-tools.ts` as the single named exemption.
  - No raw edit to `web-tools.ts`.
  - Structural scanner allowlist contains exactly `src/lib/ai/web-tools.ts` with justification.
- Updated callers and tests.
  - Store calls now send `campaignId` to Apify start and Seamless search.
  - Enrichment runners and async status/research/probe routes mint identity/probe clearances.
  - Adapter tests now mint explicit test clearances and use Node module mocks for `server-only`.
  - Added `tests/sourcing-provider-egress.mts` for prohibited-query provider mock call count.
  - Added `tests/sourcing-provider-egress-structure.mts` for raw-fetch/cast structural enforcement.
  - Updated `docs/SOURCING.md` compliance row and pinned it in `tests/docs-truth.mts`.
  - Registered the new suites in `tests/test-manifest.mjs` and updated frozen manifest baselines.

## Verification

Passed:

- `npm run typecheck` -> exit 0.
- `npm run typecheck:tests` -> exit 0.
- `npm run lint` -> exit 0 with 4 existing warnings in
  `src/components/floor3d/retro/objects/AgentModel.tsx`.
- `node --import tsx tests/test-manifest-contract.mts` -> 7 passed, 0 failed, 1 skipped.
- `node --import tsx tests/sourcing-query-policy.mts` -> 2 tests, 0 failed.
- `node --experimental-test-module-mocks --import tsx --test tests/sourcing-provider-egress.mts` -> 1 test, 0 failed.
- `node --import tsx tests/sourcing-provider-egress-structure.mts` -> 2 tests, 0 failed.
- `node --import tsx tests/docs-truth.mts` -> `RESULT docs-truth: 46 passed, 0 failed`.
- `node --experimental-test-module-mocks --import tsx tests/sourcing.mts` -> `RESULT sourcing: 51 passed, 0 failed`.
- `node --experimental-test-module-mocks --import tsx tests/apify-sourcing.mts` -> `RESULT apify-sourcing: 66 passed, 0 failed`.
- `node --experimental-test-module-mocks --import tsx tests/source-apify-auth.mts` -> `RESULT source-apify-auth: 14 passed, 0 failed`.
- `node --experimental-test-module-mocks --import tsx tests/source-demo-auth.mts` -> `RESULT source-demo-auth: 4 passed, 0 failed`.
- `node --experimental-test-module-mocks --import tsx tests/source-live-campaign-authority.mts` -> `RESULT source-live-campaign-authority: 5 passed, 0 failed`.
- `node --experimental-test-module-mocks --import tsx tests/apollo-enrichment-authority.mts` -> `RESULT apollo-enrichment-authority: 47 passed, 0 failed`.
- `git diff --check` -> exit 0.
- Raw provider-surface scan:
  `rg -n "\bfetch\s*\(" src/lib/sourcing src/app/api/source src/lib/enrichment src/lib/ai/web-tools.ts`
  found only `src/lib/sourcing/provider-egress.ts:81`.
- Clearance-cast scan:
  `rg -n "as ProviderClearance" src tests`
  found only `src/lib/sourcing/provider-egress.ts:31`.

Blocked by local sandbox:

- `npm run test:all` -> failed in `tests/apollo-cleanup-worker.mts` with:
  `error: 'listen EPERM: operation not permitted 127.0.0.1'`.
- `npm run test:database` -> failed with:
  `permission denied while trying to connect to the docker API at unix:///Users/tony/.colima/default/docker.sock`.
- `npm run test:manifest` -> failed before tests with:
  `Error: listen EPERM: operation not permitted /var/folders/5m/c_klcrrj4yj_jxhf4t6vhb080000gn/T/tsx-501/63420.pipe`.
  The equivalent `node --import tsx tests/test-manifest-contract.mts` passed.

## Blockers

1. Full `test:all` cannot complete in this sandbox because loopback listeners are denied.
2. Full `test:database` cannot complete in this sandbox because Docker/Colima socket access is denied.
3. `tsx` CLI scripts can fail in this sandbox because its IPC pipe listener is denied.

## Next steps

1. Run the full locked gate from an environment with loopback and Docker:
   `npm run typecheck && npm run typecheck:tests && npm run lint && npm run test:all && npm run test:database`.
2. If the full gate passes, review the intended Rock 2 diff and commit it.
3. Watch the stricter discovery behavior on Sillage/company-only and Seamless/company-only searches:
   those now require role-bound criteria for discovery clearance. If Owner wants a different product rule,
   make that a separate policy decision.

## Decisions made (don't relitigate)

- The prohibited regex was kept byte-identical.
- `src/lib/ai/web-tools.ts` is the only named structural exemption.
- Provider adapters require clearance rather than relying only on route-level checks.
- Discovery clearances use `validateSourcingCriteria`; identity/probe clearances exist only for non-discovery provider sockets.
- No migration was added or edited.
- `sequences_enabled` stays false and no send path was added.

## Watch out

- `ProviderClearance` is a type-level brand. The module-private cast is intentionally inside
  `provider-egress.ts`; the scanner fails any cast outside that file.
- Tests importing server-only provider modules need Node module mock mode:
  `node --experimental-test-module-mocks --import tsx ...`.
- Package scripts using the `tsx` binary can fail in this sandbox even when the `node --import tsx`
  equivalent passes.
