---
project: MSourcing / ARIA
shift: 44
agent: codex-gpt-5
updated: 2026-07-21 19:14 EDT
status: in-progress — source hardening and independent QA; not released
---

# Handoff — MSourcing / ARIA

## Current state

- Canonical clean worktree: `/Users/tony/msourcing-enterprise-go-20260721`. Do not modify the OneDrive checkout.
- Branch: `codex/enterprise-go-20260721`; base `deb81b75d8c9925476baac26297c7d2fd5760352`, tracking `origin/integration/sourcing-enrichment-on-main`.
- The shared tree is intentionally dirty with the current uncommitted enterprise slice. Preserve every uncommitted file.
- Protected GitHub target is `main`. Direct main push is prohibited by branch protection; one independent approval and last-push approval are required.
- Latest GitHub CI and CodeQL attempts for base SHA `deb81b75` started zero steps. Check-run annotations say: `The job was not started because an Actions budget is preventing further use.` Production workflow is still manually disabled. This is an account/owner gate, not a source-test failure.
- Live Fly remains an older release and is not acceptance evidence for this branch. A fresh 2026-07-21 19:19 EDT readback returned `/api/health` 200 and `/api/ready` 503 on build `3ff4852...`, migration 0046. The readiness body reports database, auth, queue, migration, and release identity true; only `agentFrameworks=false`. `aria-mantu-db` has one started Machine, `aria-mantu-auth` has two started Machines, and the application has one started web Machine plus one started loop Machine. This proves the original database/auth outage is not current, but it does not prove this branch, the new schema, framework readiness, or a real sourcing canary.
- Production AI keys exist as vault metadata, but an active approved runtime binding is not yet proven live. Do not print or copy key values.

## Done this shift

- Added authenticated, bounded, idempotent need ingress; durable requisition parsing; deterministic campaign creation; anonymous GitHub sourcing batches; source receipts; exact candidate projection; control/fence/replay handling; retention cleanup; operational readiness; protected activation; and capacity-release harnesses through migrations 0048–0057.
- Campaign/candidate compatibility proofs are green after pagination and lifecycle repair: focused TypeScript 53/53, campaign database 63/63, sourcing database 54/54, focused ESLint, syntax, whitespace, and replay checks. Integrated typechecks are green after the provider lane stabilized.
- Retention database proof is green: `bash tests/requisition-input-retention-db.sh` passed 22 assertions.
- Protected deploy contract was repaired after the cleanup receipt fixture omitted three requisition-retention counters. `node --import tsx --test tests/deploy-contract.mts` now passes 138/138.
- Full framework source group passed before runtime-image remediation. The earlier pinned Flowise worker `sha256:ff5bb...` proved the negative 503 readiness path but retained 318 HIGH/CRITICAL findings and is not promotable.
- A first 65.7 MB distroless replacement scanned at zero HIGH/CRITICAL, but independent filesystem/startup review proved `pnpm prune --prod` had removed all runtime dependencies. That scan was discarded as a false green; `MODULE_NOT_FOUND` is recorded in `_relay/codex-findings.md`.
- Provider bridge refactor is fail-closed: the browser route uses a reusable workspace-bound execution core; the reserved internal route returns 503 before egress until a durable multi-provider evidence contract exists. Anonymous GitHub 0054 remains the only autonomous provider authority.
- Provider authority review is closed in source: immutable credential identity, live non-billable authentication proof, NULL-safe activation evidence, exact Tavily tenant/provider/status/method checks at every egress boundary, and no process-key fallback. Proof: binding DB 32/32, route 29/29, provider/static 30/30, Tavily 30/30, function privilege contract 21/21, manifest 10/10, both typechecks, focused ESLint, and diff check.
- Campaign pause/fill races and deterministic continuation are closed in source: target 9, max 3 per call, ordinals 0–4, language rotation then page, one atomic continuation, exact receipts, replay-safe dedupe, Paused/Filled pre-egress stops, and post-egress pause preserves evidence without resuming.
- Functional QA is complete. Unknown equity, urgency, start date, provider counts, and exclusions are no longer presented as observed facts; unsupported autonomous roles atomically pause before egress; GitHub matches retain exact search-response relevance evidence without converting it to candidate skills. Independent focused TypeScript rerun passed 92/92, sourcing DB passed 57 assertions, and campaign DB passed 63 assertions.
- Security/privacy QA is complete. Focused need-ingress, runtime-binding, sourcing-authority, worker, loop, retention, and function-privilege suites are green. It confirmed the external edge throttle, recovery erasure journal, candidate-level memory provenance, provider erasure verification, and ordinary browser sourcing durability remain open release blockers.
- Reliability QA fixed a polling listener leak and abort-registration race in `scripts/sourcing-loop-worker.mjs`; focused worker proof passes 30/30. Recovery and fault suites are still running.
- Ordinary authenticated sourcing now stages one bounded result in migration 0058, recovers it before any new provider egress, commits an exact `sourcing-run:<run-id>:<result-sha256>` workspace receipt, acknowledges idempotently, and scrubs candidate payloads on acknowledgement, TTL expiry, or erasure. Exact actor/workspace/hash checks, zero-hit and all-skipped receipts, count-change recovery, alias-identity erasure, legal holds, rollback refusal, reapply, forced RLS, and true different-ID concurrency are covered. Both typechecks and 108 focused application tests pass. The final fresh database gate passed exactly: `behavior=pass concurrency=pass acl=pass rollback=guarded reapply=pass rows=8`.
- Sourcing-loop crash receipts no longer serialize raw exception messages; focused loop tests pass 29/29.
- The earlier zero dependency-audit result became stale when GHSA-f88m-g3jw-g9cj was published for `sharp <0.35.0`. A Next-scoped override now resolves `sharp` 0.35.3 and libvips 8.18.3. Fresh `npm audit --audit-level=moderate` is zero, the native transform passes, and Next's `optimizeImage` path produced a valid resized WebP. Full build and exact Linux image scanning remain pending. Capacity release harness passes 11/11, but it is not a 50,000-user live-capacity proof.
- Runbooks now identify protected `main` delivery as the only valid production path and retire direct Fly mutation scripts.

## Active work

1. `flowise_worker_readiness` proved the repaired current-pin runtime can resolve its production dependencies, then found two entrypoint defects and fixed them. Its complete runtime still scans red at 15 CRITICAL / 116 HIGH. The official Flowise 3.1.3 comparison is pinned to commit `09f099dbfda179e439ec677d06ad737375899eef`; its first build stopped on scoped Docker `ENOSPC`, so cleanup and the unsuppressed rebuild/scan are active.
2. QA Expert 3 (`qa_reliability_recovery`) has closed ordinary result durability and its application/database proof. Final global function, schema-digest, and manifest-fingerprint reconciliation is intentionally deferred to the lead agent until the concurrently edited migration 0054 stabilizes.
3. QA Expert 4 (`qa_capacity_release`) is auditing 50,000-user assumptions, Fly topology, backpressure, workload evidence, exact-SHA release identity, and protected activation. No live-capacity claim is allowed from fixture validation.
4. Autonomous Graphify lesson consumption remains absent. The next implementation must choose only human-promoted, exact-role, Graphify-bound, unexpired GitHub lessons already inside server-derived canonical query authority, then bind the exact lesson/version/query snapshot before egress.

## Blockers

- GitHub Actions budget prevents all hosted CI/CodeQL jobs from starting. Owner must restore Actions billing/budget, then exact-SHA runs must be rerun.
- Protected `main` requires an independent approval and last-push approval. Do not bypass or self-approve.
- The production deploy workflow is disabled and cannot be accepted until merged to the protected branch and re-enabled under its environment protections.
- Flowise has no accepted replacement image yet. Discard the dependency-empty false-green image. Do not sign, promote, deploy, or claim framework readiness unless the complete runtime starts and passes the exact zero-HIGH/CRITICAL policy.
- Public need ingress is intentionally unavailable in production until a shared edge limiter covers the custom origin and direct Fly hostname, uses trusted edge identity, and passes two-Machine burst plus valid-request tests. A Postgres limiter is not a substitute.
- Production has one real admin identity available to this work; AI runtime activation requires two distinct active tenant admins. Do not fabricate or promote a second reviewer.
- A live 50,000-user claim needs an owner-approved workload, paid production-shaped staging, synthetic session pool, telemetry, fault injection, load/spike/soak/noisy-tenant runs, and restore/failover evidence. The checked-in harness alone cannot supply this evidence.
- Live framework apps, Flowise bootstrap identity, promoted signed digests, provider canary, and exact-release Fly proof remain absent.
- Fable advisor is unavailable because the Fable 5 CLI quota is exhausted. Exact CLI response: `You've reached your Fable 5 limit. Run /usage-credits to continue or switch models with /model.`

## Next steps

1. Finish and independently review functional/security QA repairs and the complete Flowise runtime scan. Keep red release blockers explicit.
2. Rerun campaign/sourcing/provider/framework focused TypeScript and disposable-Postgres suites after shared edits stop.
3. Rebaseline function privileges and legacy schema digests only after migrations stop changing; run the privilege harness twice.
4. Run four independent QA lanes: functional/no-fake E2E, security/tenancy/privacy, concurrency/recovery, and capacity/release operations. Fix all release-blocking findings.
5. Run the complete gate from `package.json`: application/test typechecks, lint, application tests, database tests, security tests, framework tests, isolated build, dependency audit, secret scans, image/provenance checks, NUL scan, and `git diff --check`.
6. Update `_relay/codex-findings.md`, this baton, and `/Users/tony/.codex/plans/msourcing-enterprise-autopilot-closure-20260719.md` with exact final results.
7. Commit only a green coherent slice, push the branch, read back the remote SHA, and open a PR to protected `main`.
8. After the owner restores Actions budget and supplies approval, require every protected check green for the exact SHA before deployment.
9. Deploy only through the protected workflows. Apply migrations, verify exact image/ledger identities, then run a no-contact real-need canary through parse → campaign → real provider → persisted candidates. Prove zero outbound sends.
10. Do not claim production completion until `/api/ready` is 200 on the exact release, framework and loop receipts are fresh, the real canary is traceable, and capacity/restore evidence exists.

## Decisions made (do not relitigate)

- Source green, GitHub green, protected merge, Fly deployment, live provider canary, and 50,000-user capacity are separate proof surfaces.
- No synthetic or model-invented candidate is accepted. Every autonomous candidate needs observed provider evidence and stable source identity.
- Outbound sending remains disabled by default. Sourcing activation and outbound-drain activation are separate capabilities.
- Every protected application release starts the sourcing loop dark. A separate independently approved activation can turn it on only for the exact accepted release and must re-dark on failure.
- Migration 0054 is GitHub-specific. Do not disguise LLM/Tavily/multi-provider execution as GitHub evidence.
- DeerFlow and Flowise inputs remain exact-commit and immutable-digest bound. No mutable tag, ignored scanner finding, or public framework endpoint is acceptable.
- The OneDrive checkout is not the build/release lane. Use the clean non-OneDrive worktree above.

## Watch out

- Do not run formatting or cleanup across the shared tree while agents are active. Confirm ownership before editing overlapping files.
- Do not recompute privilege/schema baselines until 0054/0055 are final; otherwise the digest will become stale again.
- Do not wire `/api/internal/sourcing-execute` into the worker while its capability says `provider_evidence_unavailable`.
- Do not treat either locally built Flowise image as promotable: the upstream runtime is vulnerable and the first distroless result was dependency-empty. Require a complete fresh image plus runtime and scan proof.
- Do not use direct `fly deploy`, `fly secrets set`, or retired `scripts/prod-*` paths to bypass the protected workflow.
- Never put secret values, raw candidate payloads, or PII into Relay, logs, commits, receipts, or CI artifacts.
