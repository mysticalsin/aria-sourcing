---
project: MSourcing / ARIA
shift: 24
agent: codex-gpt-5
updated: 2026-07-11 05:23 EDT
status: local-source-qa-go-production-no-go
---

# Handoff - enterprise infrastructure and security hardening

## Current state

- Branch: `deploy/fly-github-actions`.
- `HEAD` and `origin/deploy/fly-github-actions` remain `05cda612130d941f63512d503541c0410f1fc0fd`.
- The integrated changes are uncommitted shared work. Preserve every working-tree file.
- Production verdict: **NO-GO / release frozen**. No deploy, commit, or push occurred.
- Independent source verdicts:
  - Databricks authority: ACCEPT.
  - PostgreSQL authority/ACL source: ACCEPT; real PostgreSQL execution not run.
  - Public egress and remote MCP policy: ACCEPT; focused MCP/web/sourcing review 207/207.
  - Exact application-image release chain: ACCEPT; infrastructure 83/83, deploy 34/34, bootstrap 17/17, YAML/actionlint/shell checks clean.
- Independent final QA verdict: local source GO; production NO-GO.
  - `npm run pretest`: 283/283.
  - `npm test`: lifecycle pretest 283/283, then 97 groups with 2,430/2,430.
  - Focused DB/public-fetch/Databricks: 129/129; MCP/Tavily: 114/114; release/readiness/docs: 210/210.
- Final verification after source stabilization:
  - `npm run test:security`: 495/495.
  - `npx tsc --noEmit`: exit 0.
  - `NPM_CONFIG_OFFLINE=true npm run build:isolated`: exit 0; 504 packages installed in the isolated copy and 59 static pages generated.
  - `npm run lint`, `npm audit --audit-level=high`, and `npm ls --all`: exit 0. Two moderate PostCSS advisories remain under Next.js; the local tree reports one platform-optional `@emnapi/runtime` package as extraneous.
  - Shell syntax, workflow YAML, eight TOML files, and `git diff --check`: exit 0.
  - The first root full test exposed stale hardcodes in `tests/docs-truth.mts`; the test now derives command count from `package.json` and checks a recent ISO status date. Independent QA then reran the exact tree with zero test failures.
- GitHub was rechecked without Fly credentials:
  - Repository `mysticalsin/aria-sourcing-demo` is public.
  - `Deploy Aria Mantu (Fly)` remains `disabled_manually`.
  - Default branch is `vercel-demo`; the deploy workflow is absent there.
  - `deploy/fly-github-actions` is unprotected and Production permits self-review.
- Last credential-safe production evidence: app and Kong liveness returned 200; database and Auth machines exited code 1; Auth, REST, and careers returned 503. This was not refreshed after the token incident and may have drifted.
- Incident `_relay/incidents/2026-07-11-fly-deploy-token-exposure.md` remains open. Treat the exposed Fly token as active and never use it.

## Done this shift

- Used separate Senior Full-Stack, PM/Product, Project Manager, Cybersecurity Analyst, Cybersecurity Director, database-security, QA, and independent developer/release-validator roles in sequential waves.
- Replaced push deployment with a protected, serialized, manual exact-SHA workflow gated on successful CI and CodeQL.
- Split CI into quality, dependency audit, secret scan, database security, application-image supply chain, and aggregate release jobs with immutable action pins.
- Bound the release scan to the deployed application artifact: build once, save the image, generate a CycloneDX SBOM, fail on HIGH/CRITICAL findings, publish the same image, resolve its registry digest, deploy by digest, and compare the running digest.
- Added always-run release evidence inventory/upload. Successful releases require the receipt, SBOM, SBOM checksum, vulnerability report, and image-provenance record.
- Hardened `deploy-fly.sh`, bootstrap transactions, migration-ledger identity, password-rotation resume, service readiness, immutable service images, and release receipts.
- Added `/api/ready` with bounded database-through-REST, Auth, queue, release SHA, and complete migration-ledger checks.
- Replaced the Dust SDK and removed its high-advisory dependency chain.
- Normalized Databricks connection authority into admin-only records, composite workspace/provider/key binding, revisioned audit events, explicit admin rebinding, and deployment-owned `DATABRICKS_ALLOWED_ORIGINS`.
- Added a connection-safe Node public transport with validated-address pinning, original Host/SNI, redirect/compression denial, absolute deadlines, and request/response/header/body caps.
- Made remote third-party MCP execution impossible in production even when the flag is true. Nonproduction requires exact environment plus opt-in and has tool, call, result, and time bounds.
- Closed raw, form, partial, mixed, and repeated percent-encoding Tavily-key fallback leaks.
- Reset current and future Supabase API-role privileges for both migration owner and `supabase_admin`, added explicit routine allowlists, and added real-database role/RLS/default-ACL probes.
- Updated README, status, canonical Fly runbook, Databricks playbook, enterprise audit, findings, plan, project learning, and relay archive.
- Completed independent exact-tree QA with no source edits.
- Archived shift 23 to `_relay/archive/2026-07-11-0523-codex-gpt-5.md`.

## Blockers

- Owner action is required to replace and revoke the exposed Fly token, prove old-token rejection, review activity, and install separate deployment and short-expiry single-app registry tokens.
- The public repository cannot safely receive the detailed incident and security evidence without a confidentiality decision.
- No current production backup, disposable restore, rollback receipt, audited migration-ledger baseline, measured RPO/RTO, or replica is proven.
- Database and Auth exit code 1 remains undiagnosed. Do not infer the root cause.
- Migration `0019`, bootstrap SQL, Databricks RLS cases, and final effective ACL/default-ACL matrix have not executed against a real PostgreSQL clone.
- Local Docker proof is blocked: Colima could not reach registries and Docker Desktop did not expose a daemon socket.
- The workflow is absent from the default branch, disabled remotely, and the release branch has no protection.
- The exact application image has source-level SBOM/scan binding. Database, bootstrap, and Kong custom images do not yet have equivalent per-release scan evidence.
- No image signature or SLSA-style provenance attestation exists.
- No exact committed SHA has current online CI, CodeQL, database-security, secret-scan, supply-chain, registry, Fly, or full-stack acceptance proof.
- Production `DATABRICKS_ALLOWED_ORIGINS`, admin rebinding, and one audited bounded intake are pending.
- Broader agent ownership, memory isolation, conversation routing, provider acceptance, Flowise isolation, browser E2E, accessibility, monitoring, and owner acceptance remain open.

## Next steps

1. Resolve the public-repository confidentiality gate, then form reviewable commits without discarding shared work.
2. With owner-level Fly access, replace and revoke the exposed token, prove rejection, review activity, and install separate scoped deployment and registry tokens without printing values.
3. Preserve production data and restore it into a disposable target. Reconcile migration history, then run migration `0019`, bootstrap, RLS, final ACL, and future-object default-ACL tests.
4. Reproduce database/Auth exit code 1 on the disposable target, fix the root cause, and prove two restarts.
5. Put the manual workflow on the default branch, protect the release branch, require all exact-SHA checks, and prevent self-review.
6. Run online CI, CodeQL, audit, secret scan, database security, SBOM, and image scan for one exact committed SHA.
7. Deploy the exact scanned digest and prove database, Auth, REST, Kong, app readiness, release/migration identity, running digest, restart survival, rollback, and complete receipts.
8. Rebind Databricks through an admin, run one bounded intake, and record the non-secret audit event.
9. Record owner PASS/FAIL, then continue the broader enterprise plan.

## Decisions made (do not relitigate)

- ARIA is the identity, policy, audit, and delivery control plane. Flowise is private authoring only.
- AgentSpec is the stable agent identity; runs, memory, conversations, sender binding, budgets, and audit must reference it.
- Replies route by provider context and canonical conversation, never latest outbound address or active campaign.
- Live backend failure is degraded state, never synthetic demo state.
- Liveness, deployment completion, readiness, and release acceptance are separate states.
- Human review remains the default release authority until a narrow auto-answer path has canary, kill switch, and acceptance proof.
- LinkedIn remains official-integration or assisted draft-only. No bypass work.
- Databricks execution reads normalized admin authority only. Legacy workspace JSON is never restored as authority.
- Remote third-party MCP execution is production-denied. Development/test opt-in must not use production credentials or destructive servers.
- Do not rewrite history, delete production data, or remove tracked evidence/binaries without a separate reviewed decision.

## Watch out

- Never use the current Fly token.
- Never push the incident or detailed security audit to the public origin before the confidentiality gate.
- A secret named `FLY_REGISTRY_TOKEN` is not proof of scope. Verify provider-side authority and expiry.
- `workflow_dispatch` requires the workflow definition on the default branch.
- The legacy-schema guard is intentionally fail closed. Do not invent migration-ledger rows.
- A Git-clean tree can still include ignored secrets. Keep `.dockerignore` and its contract aligned with `.gitignore`.
- `/api/health` is liveness only. Acceptance requires database, Auth, REST, complete ledger, build identity, running image digest, and `/api/ready`.
- The public-fetch test seam must remain test-only; production imports are contract-checked.
- Nonproduction remote MCP remains capable of third-party side effects by design.
- `.localbin/` contains committed macOS arm64 binaries. Repository cleanup is a separate retention decision.
