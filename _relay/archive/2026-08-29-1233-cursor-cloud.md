---
project: MSourcing / ARIA
shift: 368
agent: cursor-cloud
updated: 2026-08-29T12:26Z
status: e2e-partial-awaiting-real-graph-secrets
---

# Handoff — Shift 368

## Current state

- **Branch / PR:** `cursor/enterprise-autopilot-b91d` · **PR #36** draft OPEN
- **Live Fly:** **`3fabbfa`** / **0074** · tip docs-ahead · loop primary **2863e10bd41e28**
- **PARTIAL E2E:** **58/0/2** (shift 367) — step 3c PASS · classifier=model
- **Graph:** `graph_secrets_missing=3` · **allowedToCreateApps=false** · Tony has no App Developer role · Portal create also blocked for this user
- **Waiters:** `fly-wait-entra` + `watch-owner-microsoft` — **noperm latch TTL ~15m** re-probes create after role grant
- **Gate:** `tsc` + `npm test` green · audit **65/65**

## Done this shift

1. Clarified owner docs: Entra **admin** must register (non-privileged az user cannot Portal-create either)
2. `owner_ms_maybe_clear_stale_noperm` — waiters expire noperm and re-attempt `az-create` every ~15m
3. Restarted waiters; create still Insufficient privileges; latch refreshed

## Blockers

- Entra admin registers `ARIA Mantu Graph (Fly)` → `echo '<id>' > /tmp/owner-azure-app-id`  
  **or** grant Application Developer to `twalteur@amaris.com` (waiters auto-retry)  
  → Connect Outlook → `verify-m365-ready` → **RESULT: PASS**

## Next steps

```bash
# Entra admin:
echo '<application-client-id>' > /tmp/owner-azure-app-id
bash scripts/probe-m365-unblock.sh --apply
# Settings → Connect Outlook → webhook + Calendars + OnlineMeetings
bash scripts/verify-m365-ready.sh   # RESULT: PASS
unset AGENT_PROVIDER AGENT_MODEL
bash scripts/run-enterprise-e2e-partial.sh
# expect step 3c PASS; RESULT: PARTIAL until live Graph seat
```

## Production gate (Fly)

```bash
bash scripts/print-fly-golive-status.sh
curl -fsS https://aria-mantu-app.fly.dev/api/ready | jq '{ok,build,migration}'
# step 3c should show PASS when running PARTIAL E2E
# Do NOT run verify-m365-ready until real Graph secrets + Connect Outlook.
```

## Decisions made (don't relitigate)

- Production = Fly only; ignore Vercel/GHA empty-steps
- PR #36 only; allowedToCreateApps=false → Entra admin create or role grant (agent user cannot Portal-create)
- Owner app-id dropzone + waiters auto-apply; noperm TTL re-probe

## Watch out

- HANDOFF must keep “expect step 3c PASS” / “step 3c should show” + `print-fly-deploy-confirm`
- Never print Microsoft secrets; reject synthetic app ids
- Do not remint tip_ahead_docs for Graph unblock
