---
project: MSourcing / ARIA
shift: 266
agent: cursor-cloud
updated: 2026-08-28T10:22Z
status: tip-live-344fcaf-honesty-m365-blocked
---

# Handoff — Shift 266

## Current state

- **Live Fly tip:** `344fcaf` · migration **0071** · honesty fixes live
- **Branch tip:** `f5eab35` (relay-only ahead of live — remint not required)
- **PR:** [#35](https://github.com/mysticalsin/aria-sourcing/pull/35)
- **Gate:** local `npx tsc --noEmit && npm test` green · audit **59/59** · PARTIAL E2E 48/0/1 on live
- **GHA/Vercel:** red ~3–5s empty jobs + Vercel rate-limit — **ignore** (Fly-only; CI-BUDGET)
- **M365 secrets still missing (7)** · noperm · no `/tmp/owner-microsoft.env`
- **Watcher:** tmux `watch-owner-microsoft` + timer `m365-secrets-reprobe`

## Done this shift

1. Triaged CI failure notifications: empty steps, no logs — budget exhausted, not a code regression
2. Re-probed M365: still 7 missing; Entra ARIA-named apps still `[]`

## Blockers

1. **Owner** Entra app in Mantu Sandbox `ce57ebe3…` + 7 Fly secrets (`_relay/M365-OWNER-UNBLOCK.md`)

## Next steps

1. Owner: `bash scripts/print-m365-owner-portal-checklist.sh` → create app → apply → Connect Outlook → Enable webhook
2. `bash scripts/verify-m365-ready.sh` → RESULT: PASS (incl. 6b)
3. Loop kill switch only after full PASS

## Decisions made (don't relitigate)

- **Production = Fly only** — ignore Vercel/GitHub Actions CI
- Entra tenant = **ce57ebe3…** (Mantu Group Sandbox)
- Never invent OAuth scopes; fail closed on missing calendar/Teams scopes

## Production gate (Fly)

```bash
curl -fsS https://aria-mantu-app.fly.dev/api/ready | jq '{ok,build,migration}'
# expect build 344fcaf…, migration 0071
ARIA_ALLOW_PARTIAL_M365_E2E=1 bash e2e-workflow-test.sh
bash scripts/post-m365-secrets-golive.sh
bash scripts/verify-m365-ready.sh
```
