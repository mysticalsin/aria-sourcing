---
project: MSourcing / ARIA
shift: 23
agent: codex-gpt-5
updated: 2026-07-11 02:23 EDT
status: local-infrastructure-accepted-production-degraded-release-frozen
---

# Handoff - enterprise infrastructure hardening

## Current state

- Branch: `deploy/fly-github-actions`.
- `HEAD` and `origin/deploy/fly-github-actions` remain `05cda612130d941f63512d503541c0410f1fc0fd`.
- The integrated infrastructure hardening is uncommitted. Preserve every working-tree file as shared work.
- Local code verdict: QA GO and independent developer ACCEPT with no remaining local P0/P1/P2 findings.
- Production verdict: NO-GO. No deployment, commit, or push occurred this shift.
- Exact-tree local verification:
  - `npx tsc --noEmit && npm test`: exit 0.
  - Release contracts: deploy 30/30, bootstrap 17/17, infrastructure 61/61, readiness 9/9, Dust 32/32.
  - `npm run lint`: exit 0.
  - `NPM_CONFIG_OFFLINE=true npm run build:isolated`: exit 0 after installing 504 packages; Next compiled and generated 59 static pages.
  - `npm audit --audit-level=high`: exit 0; two moderate PostCSS advisories remain inside Next.js.
  - `npm ls --all`, shell syntax, workflow YAML 3/3, Fly TOML 6/6, and `git diff --check`: exit 0.
- GitHub release state, verified without Fly credentials:
  - Repository `mysticalsin/aria-sourcing-demo` is public.
  - `Deploy Aria Mantu (Fly)` is `disabled_manually`.
  - `Production` requires reviewer `mysticalsin` and accepts only branch `deploy/fly-github-actions`.
  - Default branch is `vercel-demo`; the deploy workflow definition is absent there, so `workflow_dispatch` cannot yet be used.
  - `deploy/fly-github-actions` has no branch protection. Administrator bypass and self-review remain possible.
- Last verified production state before the Fly credential freeze: app liveness and Kong liveness returned 200; database and Auth machines exited code 1; Auth, REST, and careers returned 503. This state was not refreshed after the token incident and may have drifted.
- Incident `_relay/incidents/2026-07-11-fly-deploy-token-exposure.md` remains open. The exposed Fly token is still active and must not be used.
- Full enterprise goal remains active. Agent authority, integration-secret isolation, safe egress, conversation identity, memory isolation, provider acceptance, recovery, and owner acceptance remain open.

## Done this shift

- Used separate Senior Full-Stack, PM/Project Manager, Cybersecurity Analyst, Cybersecurity Director, QA, and independent developer-validator roles.
- Replaced automatic push deployment with a manual, serialized, exact-SHA workflow that requires successful CI and CodeQL, a protected environment, pinned actions, and a non-secret release receipt.
- Rebuilt CI into independent quality, dependency-audit, and secret-scan jobs with an aggregate release gate; pinned CodeQL and CI actions by commit SHA.
- Rewrote `deploy-fly.sh` to use strict failure propagation, clean-tree and exact-SHA checks, allowlisted secret loading, private file modes, URI-encoded credentials, cryptographic Supabase JWT validation, single-shot remote deploys, exact HTTP 200 acceptance, and read-only failure diagnostics.
- Added immutable release tags and manifest-digest execution for bootstrap; verified one deployed digest per service and recorded digests in the release receipt.
- Replaced the bootstrap path with one serialized transaction, advisory lock, filename plus SHA ledger, checksum-change rejection, role/JWT/Auth reconciliation, and transactional exceptions when a legacy schema has no audited baseline.
- Added resumable current-or-target Postgres admin credential selection so a partial password-rotation release can be rerun.
- Added `/api/ready` with bounded checks for database through REST, Auth, queue storage, exact release SHA, latest migration, migration count, and aggregate ledger SHA.
- Added TCP health checks for database, Auth, and REST so a stopped machine cannot be accepted only because deployment finished.
- Digest-pinned production Node, database, Kong, Obscura, GoTrue, and PostgREST images.
- Removed `@dust-tt/client` and its high-advisory dependency chain. The replacement REST client validates schemas, bounds time, and enforces a streaming 2 MB byte cap with body cancellation.
- Hardened `.dockerignore` against `.env*`, backups, Supabase local state, local agent state, credentials, build output, and ignored compiler artifacts.
- Fixed validator-discovered defects in unsupported `psql \quit` semantics, full-ledger readiness, shell EOF handling, password-rotation recovery, ignored Docker context, and Dust response cleanup.
- Updated the audit, plan, findings, incident record, project learning, and relay archive with current evidence.

## Blockers

- The exposed Fly deployment token remains active. Owner-level Fly access is required to create a replacement, update protected secrets, revoke the old token, verify rejection, and review activity.
- The sole production database volume has no verified current backup, scratch restore, rollback receipt, replica, RPO, or RTO.
- Database and Auth exit code 1 has not been reproduced on a disposable target or diagnosed. Do not infer the cause.
- The existing production schema has no verified migration-ledger baseline. The new bootstrap intentionally refuses an absent or empty ledger on a detected legacy schema.
- Bootstrap SQL has not executed against a real PostgreSQL clone. Docker and a local PostgreSQL server were unavailable.
- The deploy workflow must exist on default branch `vercel-demo` before manual dispatch works. The release branch also needs protection.
- The new relay includes a credential-incident record and detailed security findings. It must not be pushed to the public repository without an explicit confidentiality decision, such as making the repository private or moving restricted evidence to an approved private store.
- The local replacement has no committed SHA and no online CI, CodeQL, secret-scan, container-build, image-scan, Fly-config, or live acceptance proof.
- Two moderate PostCSS advisories remain through stable Next.js. The audit-proposed force fix is a breaking downgrade.
- Broader application security and enterprise-readiness findings in `_relay/codex-findings.md` remain open.

## Next steps

1. With owner-level Fly access, rotate the exposed token, install separate protected environment secrets, revoke the old token, prove old-token rejection, and review activity. Do not print values.
2. Preserve the production database volume, restore it into a disposable target, and record rollback, fingerprints, RPO, and RTO before any destructive recovery action.
3. Execute the generated bootstrap plan against the restored PostgreSQL target. Audit the historical schema and establish the migration ledger only after exact file/hash reconciliation.
4. Reproduce database and Auth exit code 1 on the disposable target, fix the root cause, and prove two restart cycles.
5. Resolve the public-repository confidentiality gate before pushing: make the repository private or move restricted incident/security evidence to an approved private store without losing the required relay trail.
6. Form a reviewable commit series without discarding concurrent work. Merge the manual workflow definition to `vercel-demo` or deliberately change the default branch; protect the release branch and required checks.
7. Run online CI, CodeQL, secret scan, SBOM/image scan, and container tests for the exact committed SHA.
8. Re-enable manual deploy only after credential rotation, then prove database, Auth, REST, Kong, app liveness, full readiness, release receipt, restart survival, and rollback.
9. Continue the remaining enterprise plan in `docs/superpowers/plans/2026-07-11-aria-enterprise-remediation.md`.

## Decisions made (don't relitigate)

- ARIA is the identity, policy, audit, and delivery control plane. Flowise is private authoring only.
- AgentSpec is the stable agent employee identity; runs, memory, conversations, sender binding, budgets, and audit must reference it.
- Replies route by provider context and canonical conversation, never latest outbound address or active campaign.
- Live backend failure is degraded state, never synthetic demo state.
- Liveness, deployment completion, readiness, and release acceptance are separate states.
- Human review remains the default release authority until a narrow auto-answer path has canary, kill switch, and acceptance proof.
- LinkedIn remains official-integration or assisted draft-only. No bypass work.
- Do not rewrite history, delete production data, or remove tracked evidence/binaries without a separate reviewed decision.

## Watch out

- Never use the current Fly token. The deployment freeze is intentional.
- Never push the current incident and detailed security audit to the public origin without the confidentiality gate above.
- `workflow_dispatch` requires the workflow definition on the repository default branch. A valid workflow body on only the release branch is insufficient.
- The legacy-schema guard is intentionally fail closed. Do not bypass it or insert ledger rows without clone-based audit proof.
- A Git-clean tree can still contain ignored secrets; keep `.dockerignore` and its contract aligned with `.gitignore`.
- The deploy branch is far ahead of `vercel-demo`, and PR #3 has a large stale-base delta. Do not rewrite shared history or flatten unrelated changes.
- Querying only `/api/health` reproduces the old false-green condition. Acceptance must include Auth, REST, database, complete migration ledger, build identity, and `/api/ready`.
- Fly app secrets are app-scoped, including one-off Machines started for that app. Keep the dedicated bootstrap app and its secrets isolated.
- `.localbin/` contains committed macOS arm64 binaries. Repository cleanup is a separate retention decision.
