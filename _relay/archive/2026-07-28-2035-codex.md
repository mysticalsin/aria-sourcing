---
project: MSourcing / ARIA
shift: 60
agent: codex
updated: 2026-07-28 15:44 America/Toronto
status: rock-5-blob-independent-erasure-guard-patched-sandbox-db-proof-blocked
---

# Handoff - Shift 60

## Current state

- Branch/worktree contains the uncommitted Rock 5 data-protection repair, prior loop-event receipt fix, and this blob-independent erasure guard fix.
- No git commit was created in this shift.
- Existing committed migration files stayed byte-identical; only new migration `supabase/migrations/0052_data_protection_false_blockers.sql` is changed under `supabase/migrations`.
- Graphify remains unavailable in this checkout:
  - `graphify query "MSourcing candidate erasure workspace_state absent candidate blob scrub guard"` failed with:
    `error: graph file not found: /Users/tony/Library/CloudStorage/OneDrive-MantuGroup/Documents/Chief of Staff/Apps Source/MSourcing/graphify-out/graph.json`
  - `graphify-out/wiki/index.md` is absent.
- `sequences_enabled` was not changed by this shift.

## Done this shift

- Patched `supabase/migrations/0052_data_protection_false_blockers.sql` in place.
  - Added `workspace_state_candidate_present` as a separate flag from `workspace_state_present`.
  - Set that flag from the actual candidates-array lookup before candidate mirror fallback.
  - Kept malformed `workspace_state` as a hard `P0001` raise with `candidate workspace scrub failed`.
  - Skips `scrub_candidate_workspace_document` only when the state document is valid and the candidate is absent from the blob, leaving `workspace_state_count = 0`.
  - Leaves `unique (request_id, store_name)` intact.
  - Leaves `scrubbed_rows >= 0` intact.
  - Leaves `on conflict (request_id, store_name) do nothing` intact in `request_candidate_erasure`.
- Patched `tests/db/candidate-erasure-authority.sql`.
  - Added an assertion that a present blob candidate records `workspace_state.scrubbed_rows = 1`.
  - Preserved the blob-absent `absent-candidate-1` case and added an assertion that it records `workspace_state.scrubbed_rows = 0`.
  - Added a malformed workspace-state case that still expects SQLSTATE `P0001`.
- Added project learning to `_agent_state/codex/memory.json`.
- Archived the previous baton to `_relay/archive/2026-07-28-1544-codex.md`.

## Reviewed-schema diff claim

Public functions added by Rock 5 before this shift:

- `public.aria_job_payload_contract_ok(text,jsonb)`
- `public.redact_loop_events_for_candidate_erasure(uuid,text,text[],text[])`

Public functions altered by Rock 5 before this shift:

- `public.enqueue_aria_job(uuid,text,text,jsonb,timestamp with time zone,integer)`
- `public.reject_loop_event_mutation()`
- `public.request_candidate_erasure(uuid,uuid,text,text,uuid)`

Tables/columns/constraints changed:

- No table or column DDL was added by this shift.
- This shift changes only the body of `public.request_candidate_erasure(uuid,uuid,text,text,uuid)` inside uncommitted migration `0052`.
- It does not add or remove public functions, tables, columns, constraints, or function signatures beyond prior Rock 5 changes.
- `docker/bootstrap/legacy-baseline-invariants.sql` expected function ordering was not touched in this shift because the function set did not change.
- `docker/bootstrap/legacy-baseline-public-schema.sha256` still must be re-earned from a live dump for the earlier uncommitted public-schema changes in this Rock.

## Verification

Passed:

- `./node_modules/.bin/tsc --noEmit --incremental false` -> exit 0.
- `./node_modules/.bin/tsc -p tsconfig.tests.json --pretty false --incremental false` -> exit 0.
- `npm run typecheck` -> exit 0.
- `npm run typecheck:tests` -> exit 0.
- `npm run lint` -> exit 0 with 4 existing warnings in `src/components/floor3d/retro/objects/AgentModel.tsx`.
- `node --import tsx tests/test-manifest-contract.mts` -> 7 passed, 0 failed, 1 skipped.
- `npm run test:candidate-erasure` -> 17 passed, 0 failed.
- `node --check scripts/run-test-manifest.mjs` -> exit 0.
- `git diff --check` -> exit 0.
- `git diff --name-only -- supabase/migrations ':!supabase/migrations/0052_data_protection_false_blockers.sql'` -> no output.
- `find supabase/migrations -maxdepth 1 -name '0053*.sql' -print` -> no output.

Blocked by local sandbox:

- `npm run test:all` failed in `tests/apollo-cleanup-worker.mts` with:
  `error: 'listen EPERM: operation not permitted 127.0.0.1'`
- `npm run test:database` failed with:
  `permission denied while trying to connect to the docker API at unix:///Users/tony/.colima/default/docker.sock`
- `npm run test:manifest` failed before tests with:
  `Error: listen EPERM: operation not permitted /var/folders/5m/c_klcrrj4yj_jxhf4t6vhb080000gn/T/tsx-501/12147.pipe`
- `npm run build` failed with:
  `Error [TurbopackInternalError]: [project]/src/styles/globals.css [app-client] (css)`
  caused by:
  `creating new process`
  `binding to a port`
  `Operation not permitted (os error 1)`
- `npm run test:db-candidate-erasure` failed with:
  `permission denied while trying to connect to the docker API at unix:///Users/tony/.colima/default/docker.sock`

## Blockers

1. Candidate-erasure SQL assertions could not be executed here because Docker/Colima socket access is denied.
2. Full lifecycle tests that open loopback listeners cannot complete in this sandbox.
3. `npm run test:manifest` cannot complete through the `tsx` CLI because its IPC pipe listener is denied, although the direct `node --import tsx` manifest contract passes.
4. `npm run build` cannot complete because Turbopack attempts a denied process or port operation.

## Next steps

1. In the Visionary environment, run:
   `npm run typecheck && npm run typecheck:tests && npm run lint && npm run test:all && npm run test:database && npm run test:manifest && npm run build`.
2. Specifically verify `tests/db/candidate-erasure-authority.sql` now proves:
   - a candidate present in the workspace blob is scrubbed and records `workspace_state.scrubbed_rows = 1`;
   - `absent-candidate-1` is erased from other stores and records `workspace_state.scrubbed_rows = 0`;
   - malformed `workspace_state` still raises SQLSTATE `P0001`.
3. Re-earn `docker/bootstrap/legacy-baseline-public-schema.sha256` from the live reviewed-schema dump for the full uncommitted Rock 5 public schema.
4. Commit intended files only.

## Decisions made (don't relitigate)

- Fix uncommitted migration `0052` in place; do not add `0053`.
- Already-committed migrations remain immutable.
- Keep receipt store names constrained, not free text.
- Keep `unique (request_id, store_name)` and `on conflict (request_id, store_name) do nothing`.
- Keep `scrubbed_rows >= 0`.
- Keep `sequences_enabled` false in product paths and do not add a delivery/send path.
- Loop events stay append-only outside the narrow erasure redaction path.

## Watch out

- Do not treat local Docker, TSX IPC, loopback, or Turbopack sandbox failures as code proof failures without rerunning in the Visionary environment.
- Do not update `expected_functions` unless a function signature actually changes, and preserve the database collation order if it does.
- Do not loosen `candidate_erasure_receipts.store_name` beyond the named allowed list.
- Do not edit existing migration bytes.
