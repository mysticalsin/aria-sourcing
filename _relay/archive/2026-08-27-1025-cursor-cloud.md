---
project: MSourcing / ARIA
shift: 139
agent: cursor-cloud
updated: 2026-08-27 UTC
status: code-complete-awaiting-owner-deploy
---

# Handoff — Shift 139

## Current state

- **Branch/PR:** `cursor/e2e-graph-ingest-truth-af88` (off `cursor/enterprise-autopilot-b91d` · **#31**)
- **Tip:** Graph hiring-need body Prefer+normalize; ingest enqueue status honesty; E2E interest→calendar_book + recruiting-graph-stage probes; Entra SSO flag ≠ M365-ready
- **Local gate:** green; audit **43/43**
- **Fly live:** stale (owner deploy still required)
- **Owner blockers:** secrets + `ARIA_PROD_DEPLOY_CONFIRM` via `print-fly-deploy-confirm.sh` + deploy + E2E

## Done this shift

- `fetchGraphMessageForIngest`: Prefer text body + `normalizeGraphMessageBody` (HTML entity decode) for Mantu field lines
- `ingestNormalizedInboundEmail`: require `enqueued`/`already_enqueued` (no transport-only jobQueued)
- Settings M365 stack: SSO flag informational; ready = 3 live mailbox steps
- E2E: interest→calendar_book static proof; recruiting-graph-stage 401 + auth probe; Graph ingest / SSO honesty greps

## Next steps

1. Owner: `bash scripts/print-fly-secrets-checklist.sh`
2. Owner: activate → `bash scripts/print-fly-deploy-confirm.sh` → `fly-deploy-now.sh`
3. Owner: `print-fly-e2e-env.sh` → `e2e-workflow-test.sh` (now probes recruiting-graph-stage)
4. Agent: ready ok + `0066_*` + Graph 200 + E2E PASS → mark goal complete
5. Merge `cursor/e2e-graph-ingest-truth-af88` into PR #31 tip if not auto-merged

## Decisions made (don't relitigate)

- PR #31 supersedes closed #29 and #30
- No Fly deploy without `ARIA_PROD_DEPLOY_CONFIRM` — use `bash scripts/print-fly-deploy-confirm.sh`
- Target migration **0066**
- Mantu Fly: `AGENT_FRAMEWORKS_REQUIRED=false`
- Graph webhook: `/api/webhooks/microsoft-graph`
- Worker invokes LangGraph as stage authority (handlers keep side effects)
- Positive interest always enqueues `calendar_book` propose (autopilot draft is optional)
- Entra SSO NEXT_PUBLIC flag is not live SSO proof / not M365-ready

## Watch out

- E2E worker poll needs loop armed
- Graph checkpoints skip when `ARIA_WEB_INTERNAL_URL` unset
- recruiting-graph-stage live probe fails closed (401) until tip is deployed
