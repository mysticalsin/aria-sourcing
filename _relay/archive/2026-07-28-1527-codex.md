---
project: MSourcing / ARIA
shift: 58
agent: codex
updated: 2026-07-28 14:59 America/Toronto
status: rock-5-data-protection-local-focused-green-full-gate-blocked-by-sandbox
---

# Handoff - Shift 58

## Current state

- Branch/worktree contains an uncommitted Rock 5 data-protection repair.
- No git commit was created in this shift.
- Existing committed migration files stayed byte-identical; only new migration `supabase/migrations/0052_data_protection_false_blockers.sql` was added.
- Graphify remains unavailable in this checkout:
  - `graphify query "MSourcing Rock 5 data protection payload lawful basis candidate erasure loop_events"` failed with:
    `error: graph file not found: /Users/tony/Library/CloudStorage/OneDrive-MantuGroup/Documents/Chief of Staff/Apps Source/MSourcing/graphify-out/graph.json`
  - `graphify-out/wiki/index.md` is absent.
- `sequences_enabled` remains false in product paths. No send path was added or enabled.

## Done this shift

- Added `supabase/migrations/0052_data_protection_false_blockers.sql`.
  - Adds structural `aria_jobs` payload contract enforcement at `enqueue_aria_job`.
  - Adds loop-event erasure redaction path while preserving ordinary append-only 42501 behavior.
  - Replaces `request_candidate_erasure` so erasure can proceed when the candidate is absent from `workspace_state`.
  - Uses candidate id, email, phone, linkedinUrl, githubUrl, and sourceUrl identities for `agent_runs`, `agent_events`, and `agent_framework_sourcing_authorizations`.
- Updated `scripts/sourcing-loop-worker.mjs`.
  - Removed candidate records and reply text from job payload flow.
  - `inbound_classify` reads persisted inbound email via `read_inbound_email_for_loop`.
  - provider poll and shortlist build carry ids only and reload provider results by persisted provider run id.
- Updated API routes:
  - `src/app/api/source/apify/start/route.ts` enqueues `provider_poll` with ids only.
  - `src/app/api/cron/poll-provider-run/route.ts` can reread completed provider runs for downstream shortlist builds.
- Updated `src/lib/rules.ts`.
  - Lawful basis is now required for provider-sourced/live candidates as well as manual candidates.
  - Failure text is explicit and tells operators to record consent or legitimate interest; no default basis is invented.
- Updated tests and review controls:
  - `tests/sourcing-loop-worker.mts`
  - `tests/rules-confidential.mts`
  - `tests/loop-authority-contract.mts`
  - `tests/loop-jobs-db.sh`
  - `tests/db/candidate-erasure-authority.sql`
  - `tests/db/function-privileges.sql`
  - `docker/bootstrap/legacy-baseline-invariants.sql`
- Added project learning:
  - `_agent_state/codex/memory.json`
- Archived previous baton:
  - `_relay/archive/2026-07-28-1459-codex.md`

## Reviewed-schema diff claim

Public functions added:

- `public.aria_job_payload_contract_ok(text,jsonb)`
- `public.redact_loop_events_for_candidate_erasure(uuid,text,text[],text[])`

Public functions altered:

- `public.enqueue_aria_job(uuid,text,text,jsonb,timestamp with time zone,integer)`
- `public.reject_loop_event_mutation()`
- `public.request_candidate_erasure(uuid,uuid,text,text,uuid)`

Tables/columns changed:

- No table or column DDL changes.
- Migration-time data cleanup updates existing `public.aria_jobs.payload`, `payload_sha256`, and `updated_at` for rows that violate the new ids-only contract.

## Verification

Passed:

- `node --check scripts/sourcing-loop-worker.mjs` -> exit 0.
- `bash -n tests/loop-jobs-db.sh` -> exit 0.
- `./node_modules/.bin/tsc --noEmit --incremental false` -> exit 0.
- `./node_modules/.bin/tsc -p tsconfig.tests.json --pretty false --incremental false` -> exit 0.
- `node --import tsx --test tests/sourcing-loop-worker.mts` -> 15 passed, 0 failed.
- `node --import tsx tests/rules-confidential.mts` -> `RESULT rules: 61 passed, 0 failed`.
- `node --import tsx tests/loop-authority-contract.mts` -> `RESULT loop-authority-contract: 21 passed, 0 failed`.
- `node --import tsx tests/function-privileges-contract.mts` -> `RESULT function-privileges-contract: 21 passed, 0 failed`.
- `node --import tsx tests/test-manifest-contract.mts` -> 7 passed, 0 failed, 1 skipped.
- `node --experimental-test-module-mocks --import tsx tests/source-apify-auth.mts` -> `RESULT source-apify-auth: 20 passed, 0 failed`.
- `npm run typecheck` -> exit 0.
- `npm run typecheck:tests` -> exit 0.
- `npm run lint` -> exit 0 with 4 existing warnings in `src/components/floor3d/retro/objects/AgentModel.tsx`.
- `git diff --check` -> exit 0.
- `git diff --name-only -- supabase/migrations ':!supabase/migrations/0052_data_protection_false_blockers.sql'` -> no output.

Blocked by local sandbox:

- `npm run test:all` failed in `tests/apollo-cleanup-worker.mts` with:
  `error: 'listen EPERM: operation not permitted 127.0.0.1'`
- `npm run test:database` failed with:
  `permission denied while trying to connect to the docker API at unix:///Users/tony/.colima/default/docker.sock`
- `npm run test:manifest` failed before tests with:
  `Error: listen EPERM: operation not permitted /var/folders/5m/c_klcrrj4yj_jxhf4t6vhb080000gn/T/tsx-501/31086.pipe`
- `npm run build` failed with:
  `Error [TurbopackInternalError]: [project]/src/styles/globals.css [app-client] (css)`
  caused by:
  `creating new process`
  `binding to a port`
  `Operation not permitted (os error 1)`

## Blockers

1. Full lifecycle tests that open loopback listeners cannot complete in this sandbox.
2. Database tests cannot execute locally because Docker/Colima socket access is denied.
3. `npm run test:manifest` cannot complete through the `tsx` CLI because its IPC pipe listener is denied, although the direct `node --import tsx` manifest contract passes.
4. `npm run build` cannot complete because Turbopack attempts a denied process or port operation.

## Next steps

1. Visionary should run:
   `npm run typecheck && npm run typecheck:tests && npm run lint && npm run test:all && npm run test:database && npm run test:manifest && npm run build`.
2. Visionary should specifically inspect the 0052 dump diff for the reviewed-schema claim above.
3. Visionary should run the disposable Postgres erasure suite to prove:
   - absent-from-blob erasure proceeds,
   - loop_events redact only through erasure and still reject ordinary update/delete,
   - agent stores scrub by email, phone, linkedinUrl, githubUrl, and sourceUrl.
4. Commit intended files only.

## Decisions made (don't relitigate)

- Already-committed migrations remain immutable.
- The public enqueue path still rejects `swarm_assignment`; swarm dispatch keeps its own internal gate.
- `sequences_enabled` stays false and no delivery/send path is added.
- Operator-recorded lawful basis is required; the application does not infer one.
- Loop events stay append-only outside the narrow erasure redaction path.

## Watch out

- Do not treat the local Docker, TSX IPC, loopback, or Turbopack sandbox failures as code proof failures without rerunning in the Visionary environment.
- Do not add candidate records, reply text, search query text, or provider result bodies back into `aria_jobs.payload`.
- Do not loosen `reject_loop_event_mutation`; the only permitted update shape is the erasure redaction shape.
- Do not edit existing migration bytes.
