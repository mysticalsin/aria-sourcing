---
project: MSourcing / ARIA
shift: 6
agent: codex
updated: 2026-07-09 23:43
status: tavily-location-fix-pass-verified
---

# Handoff — Tavily key + location fix pass

## Current state
- Level-10 fix pass is implemented in the working tree, uncommitted.
- Hermes chat now resolves the stored Tavily key only when the caller can `source`; viewers/non-source callers get no stored workspace Tavily key attached.
- `resolveStoredTavilyKey()` now normalizes decrypt failure from `""` to `null`, preserving the env-key fallback path.
- GitHub location qualifiers generated by the app and live smoke script now quote the city value.
- `npm test` now includes `tests/web-tavily-key.mts`, `tests/intake-location.mts`, and `tests/integrations-honesty.mts`.
- `undici` is removed from `package.json` and `package-lock.json`; `npm install --package-lock-only --ignore-scripts` was run.

## Done this shift
- Patched `src/app/api/hermes/chat/route.ts` for the `source` permission gate around stored Tavily key resolution.
- Patched `src/lib/sourcing/tavily.ts` so decrypt failure returns `null`.
- Patched `src/lib/store.ts` and `scripts/smoke-source-live.mts` for quoted GitHub `location:` qualifiers.
- Added the decrypt-failure/env-fallback assertion to `tests/web-tavily-key.mts`.
- Added the three new tests to the package test chain.
- Removed the dead `undici` dependency from package metadata.
- Archived the previous baton to `_relay/archive/2026-07-09-2343-codex.md`.

## Blockers
- No code blockers in this sandbox.
- Plain `npx tsx ...` tests were not run here because prior relay evidence says bare `tsx` hits IPC `listen EPERM` in this sandbox; Visionary is expected to rerun those outside the sandbox.

## Next steps
1. Visionary runs `npx tsx tests/web-tavily-key.mts` outside the sandbox.
2. Visionary runs `npx tsx tests/intake-location.mts` outside the sandbox.
3. Visionary runs `npx tsx tests/integrations-honesty.mts` outside the sandbox.
4. Visionary runs `npx tsx tests/linkedin-policy.mts` and `npx tsx tests/dispatch-outbound.mts` outside the sandbox.
5. Review dirty working tree carefully before commit; many files predate this fix pass.

## Decisions made (don't relitigate)
- Do not attach the stored workspace Tavily key to non-source chat callers.
- A decrypt failure for a stored Tavily row means no stored key, not an empty key that shadows env fallback.
- Quote GitHub location qualifier values.
- Keep web-tools Supabase-free.
- Do not change SSRF guard, LinkedIn wire-enforcement, outreach guardrails, or broader auth scopes.

## Watch out
- `src/lib/sourcing/tavily.ts`, `tests/web-tavily-key.mts`, `tests/intake-location.mts`, `tests/integrations-honesty.mts`, and `scripts/smoke-source-live.mts` were already untracked/dirty before this pass.
- `package-lock.json` already had unrelated lockfile churn before this pass; this pass specifically removed the root `undici` dependency and lock entry.
- `npx tsc --noEmit` passed with exit 0.
