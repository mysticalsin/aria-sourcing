---
project: MSourcing / ARIA
shift: 4
agent: codex
updated: 2026-07-09 23:27
status: ready-for-visionary-live-smoke
---

# Handoff — ARIA Rock 1 GitHub people sourcing

## Current state
- Rock 1 code is implemented in the working tree, uncommitted.
- `parseEmailAndJD` now extracts a concrete `jobAnalysis.location` from pasted text patterns including `team in London`, `based in <City>`, `location: <City>`, and capitalized `in <City>`.
- `searchGithubUsers` now enforces `type:user` at the shared GitHub sourcing chokepoint unless the caller already supplied any `type:` qualifier.
- The client GitHub sourcing path appends `location:<city>` from `campaign.jobAnalysis.location` when the selected query lacks an existing `location:` qualifier.
- `scripts/smoke-source-live.mts` builds `language:<skill> location:<city>`, relies on `searchGithubUsers` for `type:user`, prints returned candidates, and asserts canonical `https://github.com/<login>` URLs.
- `tests/intake-location.mts` exists and asserts a JD saying `team in London` parses `jobAnalysis.location === "London"`.

## Done this shift
- Updated `src/lib/types.ts` with optional `JobAnalysis.location`.
- Updated `src/lib/mock-ai.ts` with focused location extraction for general intake parsing.
- Updated `src/lib/sourcing/github.ts` with `type:user` enforcement and no duplicate `type:` behavior.
- Updated `src/lib/store.ts` GitHub query construction to add parsed location when present.
- Updated `scripts/smoke-source-live.mts` to print candidates and enforce real canonical GitHub profile URLs.
- Added `tests/intake-location.mts`.
- Added regression assertions to `tests/sourcing.mts` for default `type:user` and no duplicate caller `type:`.

## Blockers
- No code blockers in this sandbox.
- Plain `npx tsx ...` proof commands are still expected to be run by Visionary outside this sandbox because previous shifts proved `tsx` IPC socket startup fails here with `listen EPERM`.

## Verification
- `node --import tsx tests/intake-location.mts` -> `RESULT intake-location: 1 passed, 0 failed`
- `node --import tsx tests/sourcing.mts` -> `RESULT sourcing: 43 passed, 0 failed`
- `npx tsc --noEmit` -> exit 0

## Next steps
1. Visionary runs `npx tsx tests/intake-location.mts` outside the sandbox.
2. Visionary runs `npx tsx scripts/smoke-source-live.mts` outside the sandbox with `GITHUB_TOKEN` and network access; expected live proof is at least 3 canonical GitHub user candidates.
3. Commit only the Rock 1 files plus the baton/archive after reviewing the pre-existing Rock 2 and unrelated dirty files.

## Decisions made (don't relitigate)
- Keep LinkedIn wire-enforcement and outreach guardrails untouched.
- Do not add scraping or alternate sourcing backends.
- Keep GitHub public-profile mapping unchanged; the people/org fix belongs in the search query via `type:user`.
- Preserve caller-specified `type:` qualifiers for backward compatibility.

## Watch out
- This worktree already had many unrelated dirty files before Rock 1, including Rock 2 Tavily work, package files, floor3d files, and untracked assets. Do not revert or bundle them blindly.
- `src/lib/types.ts` already contained an unrelated dirty `Tavily` provider addition before this shift; Rock 1 only added optional `JobAnalysis.location` there.
