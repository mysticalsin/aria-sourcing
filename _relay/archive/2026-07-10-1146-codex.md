---
project: MSourcing / ARIA
shift: 17
agent: codex
updated: 2026-07-10 11:38
status: r1-fleet-seats-server-tsc-clean
---

# Handoff - R1 Fleet Seats Server Lifecycle

## Current state
- Release Rock R1 fleet-seat blocker is implemented in source.
- Live fleet seat creation now calls `/api/fleet/seats` before committing UI state, and adopts the `agent_seats.id` UUID returned by the server.
- In Supabase live mode, hydration reads `agent_seats` and merges rows into `state.seats`; the server row is authoritative for `id`, `operator_email`, `mode`, provider/status/domain/connection fields, and workspace-state keeps display-only fields.
- Demo mode keeps local seat IDs and local-only behavior unchanged.
- `ensure_workspace()` has migration `supabase/migrations/0018_first_admin.sql`: the first profile for a newly-created workspace becomes `admin`; joining an existing workspace does not elevate role.

## Done this shift
- Added `src/app/api/fleet/seats/route.ts`:
  - `POST` creates `agent_seats` rows scoped to `ensure_workspace()`.
  - `PATCH` syncs server-relevant seat updates: `operator_email`, `mode`.
  - `DELETE` deletes a UUID seat by id.
  - Route is auth-first, `manage_fleet` RBAC-gated, `prodFailClosed`, rate-limited, and zod-validated.
- Added `src/lib/fleet-seats.ts` for row mapping, live create, live patch, and hydration merge.
- Updated `src/lib/supabase/workspace.ts` with `loadRemoteAgentSeats()`.
- Updated `src/lib/store.ts` so live add/connect/toggle paths synchronize to `agent_seats`.
- Updated fleet/persona UI callers for async seat creation and live toggle/connect results.
- Added `tests/fleet-seats-server.mts` and wired it into `npm test`.
- Archived previous baton to `_relay/archive/2026-07-10-1138-codex.md`.
- Added project-local Codex learning in `_agent_state/codex/memory.json`.

## Blockers
- No source blocker found in this sandbox.
- Live Supabase migration/application and real OAuth/domain/send/calendar verification remain owner/Visionary deployment gates outside this sandbox.

## Verification
- `graphify query "MSourcing fleet seats agent_seats store hydration ensure_workspace RBAC" --budget 1500` failed before raw inspection because `graphify-out/graph.json` is absent.
- `node --experimental-test-module-mocks --import tsx tests/fleet-seats-server.mts` passed:
  - `RESULT fleet-seats-server: 10 passed, 0 failed`
- `npx tsc --noEmit` passed with exit 0.
- `git diff --check -- package.json src/lib/fleet-seats.ts src/app/api/fleet/seats/route.ts src/lib/supabase/workspace.ts src/lib/store.ts src/app/fleet/page.tsx src/components/soul/persona-editor.tsx src/components/fleet/seat-card.tsx supabase/migrations/0018_first_admin.sql tests/fleet-seats-server.mts` passed with exit 0.

## Next steps
1. Visionary applies `supabase/migrations/0018_first_admin.sql` to the live Supabase project.
2. Visionary runs the live pasted-need path: create a fleet seat, connect OAuth, verify domain, live send, calendar booking.
3. Review and commit the R1 files separately from pre-existing dirty-tree content.

## Decisions made (don't relitigate)
- Server gates for OAuth/send/domain/calendar remain intact; the fix is making UI-created live seats real `agent_seats` rows.
- Demo mode remains local and keeps `seat_*` IDs.
- `agent_seats` is the live roster authority; workspace-state only decorates rows for UI fields not present in the table.
- First-user admin is implemented only inside `ensure_workspace()` when a new workspace is created; no RLS or anti-escalation policy was weakened.

## Watch out
- The working tree had unrelated pre-existing changes before this shift:
  - `.claude/scheduled_tasks.lock`
  - `src/lib/ai/tool-loop.ts`
  - `.rocket-fuel/`
  - `Aria/`
  - `docs/superpowers/plans/`
  - `graphify-out/`
  - `kimi-live-chat-proof.png`
- This shift intentionally touched only the R1 fleet-seat files, `package.json`, `_relay/`, and `_agent_state/codex/memory.json`.
