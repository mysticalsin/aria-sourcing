---
project: MSourcing / ARIA
shift: 9
agent: codex
updated: 2026-07-10 07:55
status: rock-4-linkedin-compliant-web-discovery-tsc-clean
---

# Handoff - Rock 4 LinkedIn Compliant Web Discovery

## Current state
- Rock 4 deliverables are implemented in the working tree, uncommitted.
- LinkedIn discovery now enforces `site:linkedin.com/in` scoping through `ensureWebQueryScope()` before web search dispatch.
- The scoping helper is idempotent, so already scoped queries are preserved rather than double-prefixed.
- LinkedIn web-lead extraction remains sparse SERP-title/snippet/URL extraction only.
- No LinkedIn profile-page fetch, page parsing, login, browser automation, or scraping path was added.
- LinkedIn outbound policy remains assisted-manual only.
- `tests/web-leads.mts` now covers the Rock 4 contract.

## Done this shift
- Read the prior `_relay/HANDOFF.md`, vault rules, Tony's recent Codex learnings, and project memory.
- Attempted required Graphify navigation first:
  - `graphify query "MSourcing ARIA LinkedIn web-leads linkedin-policy outreach guardrails Tavily site linkedin.com/in"` failed because `graphify-out/graph.json` is absent.
  - `graphify-out/wiki/index.md` is absent.
- Inspected:
  - `src/lib/sourcing/web-leads.ts`
  - `src/lib/linkedin-policy.ts`
  - `src/app/api/source/route.ts`
  - `src/lib/mock-ai.ts`
  - `src/lib/store.ts`
  - `src/lib/ai/sourcing-tools.ts`
  - `tests/linkedin-policy.mts`
  - `tests/outreach-guardrails.mts`
  - existing sourcing test patterns
- Added `ensureWebQueryScope()` in `src/lib/sourcing/web-leads.ts`.
- Updated the API source route to pass the scoped query to `runWebTool("web_search", ...)`.
- Updated the agent sourcing tool to pass the scoped query to `web_search` and to `mapWebSearchCandidates`.
- Updated the client sourcing path to store/use a scoped web-source query for mapped candidates.
- Added `tests/web-leads.mts` with assertions for scoped LinkedIn search, sparse extraction, blank contact fields, live provenance, policy guardrails, and no `fetch_page` LinkedIn profile path.

## Blockers
- `npx tsx tests/web-leads.mts` cannot start inside this sandbox:
  - `Error: listen EPERM: operation not permitted ... /tsx-501/31573.pipe`
- This matches the known sandbox limitation from prior shifts. Outside the sandbox, Visionary should run the exact requested `npx tsx tests/web-leads.mts`.

## Verification
- `node --import tsx tests/web-leads.mts` passed:
  - `RESULT web-leads: 21 passed, 0 failed`
- `node --import tsx tests/linkedin-policy.mts` passed:
  - `RESULT linkedin-policy: 18 passed, 0 failed`
- `node --import tsx tests/outreach-guardrails.mts` passed:
  - `RESULT outreach-guardrails: 42 passed, 0 failed`
- `npx tsc --noEmit` passed with exit 0.
- `git diff --check -- src/lib/sourcing/web-leads.ts src/app/api/source/route.ts src/lib/ai/sourcing-tools.ts src/lib/store.ts tests/web-leads.mts` passed with exit 0.
- Search audit found no direct `linkedin.com/in` profile fetch path in the edited discovery modules.

## Next steps
1. Visionary runs `npx tsx tests/web-leads.mts` outside this sandbox.
2. Visionary may also run `npx tsx tests/linkedin-policy.mts` and `npx tsx tests/outreach-guardrails.mts` outside this sandbox for exact-command proof.
3. Review the dirty working tree carefully before commit; many unrelated files were already dirty before this shift.
4. Commit Rock 4 separately from Rock 5, candidate-agent-security, Tavily-key, and other unrelated working-tree changes if possible.

## Decisions made (don't relitigate)
- LinkedIn discovery is allowed only through site-scoped public web search and sparse SERP extraction.
- LinkedIn profile pages must not be fetched or parsed directly.
- LinkedIn outreach remains assisted-manual only unless a separately approved official LinkedIn integration is implemented.
- Email and location stay blank for web-search LinkedIn leads unless a separate compliant enrichment step provides them.
- Do not weaken `src/lib/linkedin-policy.ts`, `tests/linkedin-policy.mts`, or `tests/outreach-guardrails.mts`.

## Watch out
- The working tree has substantial pre-existing unrelated changes, including `package.json`, `package-lock.json`, `src/lib/store.ts`, `src/app/api/source/route.ts`, and several test/source files.
- `_relay/archive/2026-07-10-0755-codex.md` archives the previous shift 8 baton.
- `npx tsc --noEmit` may update `tsconfig.tsbuildinfo` in the working tree.
