---
project: MSourcing / ARIA
shift: 269
agent: cursor-cloud
updated: 2026-08-28T10:35Z
status: e2e-approve-retry-m365-blocked
---

# Handoff — Shift 269

## Current state

- **Live Fly tip:** `344fcaf` · migration **0071**
- **Branch tip:** post `fix(e2e): retry LinkedIn approve on LLM quality 422/503`
- **PR #35** (supersedes closed #29–#33)
- **Gate:** audit **59/59** · `npm test` green
- **Strict E2E (env -u partial):** 49 pass / **2 fail** — expected until M365:
  - `microsoftOAuth=false` (no MICROSOFT_CLIENT_*)
  - no live Graph seat for **6b** Teams confirmLive
- **PARTIAL E2E:** 48 pass / 0 fail / 1 warn (6b skip only)
- **M365:** 7 secrets missing · noperm · watcher active

## Done this shift

1. Strict E2E fail-closed verified (microsoftOAuth + 6b) with partial flags unset
2. E2E approve retry loop for LLM quality 422/503 (up to 3 attempts)

## Blockers

1. Owner Entra app + 7 Fly secrets (`_relay/M365-OWNER-UNBLOCK.md`)

## Next steps

1. Owner apply secrets → watcher/post-m365-golive → Connect Outlook → webhook
2. `bash scripts/verify-m365-ready.sh` → strict PASS (incl. 6b)
3. Loop kill switch only after full PASS

## Decisions made (don't relitigate)

- **Production = Fly only** — never pretends full enterprise PASS while 6b skipped
- Strict E2E must use `env -u ARIA_ALLOW_PARTIAL_M365_E2E` (verify-m365-ready does this)

## Production gate (Fly)

```bash
bash scripts/print-fly-golive-status.sh
bash scripts/print-fly-deploy-confirm.sh
curl -fsS https://aria-mantu-app.fly.dev/api/ready | jq '{ok,build,migration}'
ARIA_ALLOW_PARTIAL_M365_E2E=1 bash e2e-workflow-test.sh
# step 3c should show PASS; MS-gap PARTIAL only when FAILS=0
env -u ARIA_ALLOW_PARTIAL_M365_E2E bash e2e-workflow-test.sh   # fails until M365
bash scripts/verify-m365-ready.sh
```
