---
project: MSourcing / ARIA
shift: 3
agent: codex
updated: 2026-07-09 23:16
status: blocked
---

# Handoff — ARIA Tavily stored-key Rock 2

## Current state
- Rock 2 code is implemented in the working tree, uncommitted.
- `web-tools.ts` remains Supabase-free. `runWebTool(name, args, { tavilyKey })` now threads an optional stored Tavily key into `web_search`; absent key still falls back to `process.env.TAVILY_API_KEY`, then DuckDuckGo.
- Stored Tavily resolution lives in `src/lib/sourcing/tavily.ts` and mirrors the Apollo `api_keys` service-role pattern with provider `"Tavily"`.
- `DATA_ENCRYPTION_KEY` is present in `.env.local`; the value was not printed.
- The exact required proof command `npx tsx tests/web-tavily-key.mts` is blocked by this sandbox before the test runs because Unix-domain socket `listen()` returns `EPERM` for `tsx` IPC pipes.

## Done this shift
- Added `"Tavily"` to `API_KEY_PROVIDERS` and Tavily `tvly-...` format validation.
- Hardened `encryptionRequiredButMissing()` so Supabase-enabled, non-demo real-data workspaces require `DATA_ENCRYPTION_KEY` outside production too.
- Threaded Tavily key through:
  - `src/app/api/source/route.ts`
  - `src/app/api/sourcing-agent/route.ts`
  - `src/app/api/agents/run/route.ts`
  - `src/app/api/hermes/chat/route.ts`
  - `src/lib/ai/tool-loop.ts`
  - `src/lib/ai/sourcing-tools.ts`
  - `src/lib/ai/web-tools.ts`
- Added `tests/web-tavily-key.mts`.
- Verification passed with the non-IPC loader:
  - `node --import tsx tests/web-tavily-key.mts` → `RESULT web-tavily-key: 10 passed, 0 failed`
  - `npx tsc --noEmit` → exit 0
  - `node --import tsx tests/linkedin-policy.mts` → `RESULT linkedin-policy: 18 passed, 0 failed`
  - `node --import tsx tests/outreach-guardrails.mts` → `RESULT outreach-guardrails: 42 passed, 0 failed`
  - `node --import tsx tests/dispatch-outbound.mts` → `RESULT dispatch-outbound: 79 passed, 0 failed`

## Blockers
- `npx tsx tests/web-tavily-key.mts` cannot start in the current sandbox:
  - Error: `listen EPERM: operation not permitted /var/folders/.../tsx-501/<pid>.pipe`
  - Repro: plain `node -e` Unix-socket `listen('/private/tmp/...sock')` also returns `EPERM`
  - This blocks satisfying Tony's exact proof-command requirement, even though the test passes through `node --import tsx`.

## Next steps
1. Re-run `npx tsx tests/web-tavily-key.mts` in a shell that allows Unix-domain socket listen, or change the project test runner to the already-working `node --import tsx` form.
2. If exact command passes, commit the Tavily Rock 2 changes only; do not include pre-existing dirty files (`package.json`, `package-lock.json`, `.claude/scheduled_tasks.lock`, floor3d files, etc.) unless Tony/Claude explicitly owns that commit.
3. Optional: add `tests/web-tavily-key.mts` to `npm test` after the exact standalone proof command passes.

## Decisions made (don't relitigate)
- Keep `web-tools.ts` Supabase-free.
- Use stored Tavily key first, then `process.env.TAVILY_API_KEY`, then existing DuckDuckGo fallback.
- Preserve route RBAC/auth gates exactly as they were; resolve Tavily only after a Supabase session exists.
- Preserve public-demo plaintext escape hatch; real-data Supabase workspaces fail closed without encryption.

## Watch out
- This worktree had unrelated dirty files before this shift. Do not clean or revert them as part of Tavily.
- `npx tsx` failure is not a test failure; it is a sandbox IPC/socket failure. Use `node --import tsx` to exercise tests here.
