---
project: MSourcing / ARIA
shift: 12
agent: codex
updated: 2026-07-10 09:15
status: wave2-rock-w1-kpi-truth-tsc-clean
---

# Handoff - Wave 2 Rock W1 KPI Truth

## Current state
- Canonical real-send KPI derivation now lives in `src/lib/metrics.ts`.
- `isRealSendFact(message)` is the single predicate for real sends:
  - `message.dryRun === false`
  - `message.sentAt != null`
- `realFunnelFacts(state, { live, campaignId? })` derives:
  - `contacted` from distinct candidate ids with real send facts.
  - `repliedCount` and `positiveReplies` from replies tied to contacted candidates.
  - `booked` from bookings tied to contacted candidates.
  - Live mode excludes `candidate.provenance === "synthetic"` from eligible candidates and downstream counts.
- Stage high-water helpers remain in `metrics.ts` for genuine pipeline-stage semantics:
  - `effectiveStageRank`
  - `funnelForCandidates`
  - stage fallback inside `computeCampaignMetrics` when no real-fact input is supplied.

## Done this shift
- Attempted required Graphify navigation:
  - `graphify query "MSourcing Wave-2 metrics canonical real send fact contacted KPI"` failed because `graphify-out/graph.json` is absent.
  - `graphify-out/wiki/index.md` is absent.
- Added canonical metrics exports in `src/lib/metrics.ts`:
  - `isRealSendFact`
  - `realFunnelFacts`
  - `missionControlHudValues`
- Updated `globalKpis` to consume `realFunnelFacts` for `contacted`, `replyRate`, and `interviewsBooked`.
- Updated live dashboard counts to exclude synthetic candidates from `candidatesSourced`, `pendingApprovals`, and `hotReplies`.
- Updated `src/lib/store.ts`:
  - `recomputeMetrics` now passes campaign-scoped `realFunnelFacts` into `computeCampaignMetrics`.
  - `useDashboardKpis` now passes `bookings` and `settings` into `globalKpis`.
- Updated `src/components/floor/mission-control-hud.tsx`:
  - Removed direct `ledger.status === "sent"` contacted counting.
  - Uses `missionControlHudValues`, backed by the same canonical derivation.
- Added `tests/metrics-canonical.mts` with the three requested traps:
  - Demo dry-run message with ledger `status: "sent"`.
  - Live approved-unsent message with `dryRun: false`, `sentAt: null`, ledger `status: "claimed"`.
  - Synthetic candidate with a real send fact.
  - The test asserts all three are excluded from live contacted and that real completed send counts.
  - The test asserts `globalKpis`, `computeCampaignMetrics`, and HUD derivation agree.
- Archived previous baton to `_relay/archive/2026-07-10-0915-codex.md`.

## Blockers
- None for Rock W1 source/typecheck.

## Verification
- `npx tsc --noEmit` passed with exit 0.
- `node --import tsx tests/metrics-canonical.mts` passed:
  - `RESULT metrics-canonical: 21 passed, 0 failed`
- `node --import tsx tests/scoring-metrics.mts` passed:
  - `RESULT scoring: 160 passed, 0 failed`
- `git diff --check -- src/lib/metrics.ts src/lib/store.ts src/components/floor/mission-control-hud.tsx tests/metrics-canonical.mts` passed with exit 0.

## Next steps
1. Visionary can run `npx tsx tests/metrics-canonical.mts` outside this sandbox.
2. Review and commit Rock W1 separately from the unrelated dirty working-tree files.
3. Continue Wave 2 with Rock W2 or W4 after W1 review.

## Decisions made (don't relitigate)
- Ledger `status: "sent"` alone is not a real-send fact because demo dry-run approvals can write that status.
- `dryRun: false` alone is not a real-send fact because live approval can exist before provider/manual delivery.
- Synthetic candidates are excluded from live KPI counts, even if they have otherwise real-looking send facts.
- This shift changed measurement only. It did not change approval, provider-send, LinkedIn manual confirmation, or guardrail behavior.

## Watch out
- The working tree had substantial pre-existing unrelated changes before this shift.
- `tests/metrics-canonical.mts` is new and not yet wired into the long `npm test` chain; Rock W5 owns full gate wiring.
- Direct `npx tsx ...` may still fail in this sandbox with the known named-pipe EPERM issue from prior shifts. `node --import tsx ...` worked for focused verification here.
