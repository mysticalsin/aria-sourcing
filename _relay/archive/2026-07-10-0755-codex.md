---
project: MSourcing / ARIA
shift: 8
agent: codex
updated: 2026-07-10 07:50
status: rock-5-google-oauth-docs-and-redirect-test-tsc-clean
---

# Handoff - Rock 5 Google OAuth Mailbox Setup

## Current state
- Rock 5 deliverables are implemented in the working tree, uncommitted.
- No OAuth route logic was changed.
- `production-readiness/GOOGLE_OAUTH_SETUP.md` now documents:
  - Google Cloud Console path: APIs & Services > Credentials > OAuth client ID.
  - Web application client type.
  - Gmail API enablement.
  - Required scopes: `gmail.send`, `gmail.readonly`, `calendar.events`.
  - Authorized redirect URIs for local ports 3003 and 3000, plus production `https://<domain>/auth/google/callback`.
  - Consent screen test-user versus verification notes.
  - Required env vars: `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET`, `GOOGLE_REDIRECT_URI`.
  - Microsoft / Entra equivalent pointer.
- `.env.local.example` and `.env.production.example` now point to the Google OAuth setup runbook and clarify the Google scopes/redirect URI requirements.
- `tests/auth-google-redirect.mts` invokes `src/app/auth/google/route.ts` and asserts a configured GET redirects to `accounts.google.com` with Gmail send/read scopes, calendar events scope, state, and PKCE.

## Done this shift
- Read the prior `_relay/HANDOFF.md`, vault rules, Graphify skill, and Tony's recent Codex learnings.
- Attempted Graphify navigation first:
  - `graphify query "MSourcing ARIA Google OAuth auth/google GOOGLE_CLIENT_ID redirect test"` failed because `graphify-out/graph.json` is absent.
  - `graphify-out/wiki/index.md` is absent.
- Inspected:
  - `src/app/auth/google/route.ts`
  - `src/app/auth/google/callback/route.ts`
  - `src/app/auth/microsoft/route.ts`
  - `src/app/auth/microsoft/callback/route.ts`
  - `src/lib/supabase/server.ts`
  - `src/lib/server/demo-side-effects.ts`
  - `.env.local.example`
  - `.env.production.example`
  - existing `.mts` test patterns
- Added the Google OAuth production setup runbook.
- Clarified Google env example comments without adding real secrets.
- Added the focused Google redirect test.

## Blockers
- `npx tsx tests/auth-google-redirect.mts` cannot start inside this sandbox. It fails before user test code with:
  - `Error: listen EPERM: operation not permitted ... /T/tsx-501/...pipe`
- This matches the existing sandbox limitation from previous shifts. Outside the sandbox, Visionary should run the exact requested `npx tsx tests/auth-google-redirect.mts`.

## Verification
- `npx tsc --noEmit` passed with exit 0.
- `git diff --check -- production-readiness/GOOGLE_OAUTH_SETUP.md .env.local.example .env.production.example tests/auth-google-redirect.mts` passed with exit 0.
- Deliverable grep confirmed the Google env vars, localhost 3003/3000 callbacks, Gmail scopes, and calendar events scope appear in the new/edited files.
- Runtime sanity check using the same test file through `node --import tsx tests/auth-google-redirect.mts` passed:
  - `RESULT auth-google-redirect: 12 passed, 0 failed`

## Next steps
1. Visionary runs `npx tsx tests/auth-google-redirect.mts` outside this sandbox.
2. Review the dirty working tree carefully before commit; many unrelated files were already dirty before this shift.
3. Commit Rock 5 separately from candidate-agent-security, Tavily/location, and other unrelated working-tree changes if possible.

## Decisions made (don't relitigate)
- Do not weaken `requireAdmin()` in `/auth/google`.
- Do not weaken the public-demo side-effects guard.
- Do not change Google scopes as part of documentation/test coverage.
- Do not add real OAuth client secrets to examples or docs.
- The test uses the existing non-production local admin harness so it can run with plain `tsx`; route logic remains unchanged.

## Watch out
- The working tree has substantial pre-existing unrelated changes, including `package.json`, `package-lock.json`, and many source/test files.
- `_relay/archive/2026-07-10-0750-codex.md` archives the previous shift 7 baton.
- `npx tsc --noEmit` updated `tsconfig.tsbuildinfo` in the working tree.
