---
project: MSourcing / ARIA
shift: 199
agent: cursor-cloud
updated: 2026-08-27T21:44Z
status: demo-ux-plus-0068-integrated-awaiting-owner-redeploy
---

# Handoff — Shift 199

## Current state

- **Branch tip:** `cursor/enterprise-autopilot-b91d` — merge of demo UX (`a679c18` lineage: `4ff518b`/`393a4be`/`a679c18`) + parse-404 (`9da085d`/`bba23f6`: migration **0068** + DeepSeek)
- **PR #32** remains **CLOSED** without merge — do not reopen; tip push is enough for owner redeploy
- **Live Fly** still **`635eb4e`** — tip-only until owner redeploys with matching confirm (do not invent confirm / redeploy)
- Live `apply_workspace_patch` still HTTP 404 / PG `42883` `digest(text, unknown)` until **0068** applied on DB
- Confirm unlock: `bash scripts/print-fly-deploy-confirm.sh` → `ARIA_PROD_DEPLOY_CONFIRM`
- Microsoft path **SKIPPED** (owner) — no Graph/Outlook/live Teams E2E; goal stays active

## Done this shift

1. Merged `cursor/fix-requisition-parse-404-cadd` into `cursor/enterprise-autopilot-b91d` (preserve both demo UX + 0068)
2. **Demo UX (already on tip):** intake title/headcount; dry-run without mailbox; shell-first hard reload `/`
3. **0068 digest path:** `supabase/migrations/0068_apply_workspace_patch_digest_path.sql` — restore `extensions` search_path + schema-qualified digest; worker `rpc_http_404:digest_unresolved`
4. **DeepSeek:** `DEEPSEEK_API_KEY` in fly-apply-owner-llm-secrets + checklist/missing/activate/example
5. Related tests: loop-authority 0068 pin; sourcing-loop-worker classify; audit matrix DeepSeek + 0068

## Blockers

- Owner must redeploy tip matching HEAD with confirm — agent must not invent confirm
- Live DB broken for campaign materialization until **0068** applied (migrate + tip deploy, or migrate-only first)
- PR #32 closed — land path TBD by owner
- MS Graph / live Outlook E2E still skipped

## Next steps

1. Owner: drop deploy confirm → apply migrations through **0068** + deploy `cursor/enterprise-autopilot-b91d` tip matching HEAD
2. Re-verify live: Java brief title+openings=3; Outreach Queue Summary Dry-run; hard reload `/`; `apply_workspace_patch` no digest 42883; webhook campaign materialization
3. Optional: `/tmp/owner-llm.env` with `KIMI_API_KEY` and/or `DEEPSEEK_API_KEY` → `bash scripts/fly-apply-owner-llm-secrets.sh`
4. Do not Approve/send outreach; do not invent Microsoft secrets; do not resume Microsoft path

## Decisions made (don't relitigate)

- Owner closed #32 — accept; push tip without fighting reopen
- Owner skip Microsoft — still in force
- Force Dry-run when no real mailbox account
- Demo UX + 0068 now co-located on `enterprise-autopilot-b91d` (integration complete)
- Local gate = CI authority; never invent deploy confirm
- VSS plain text/HTML is production baseline; PDF OCR deferred

## Watch out

- Applying 0068 without updating `ARIA_EXPECTED_MIGRATION*` / tip deploy can desync `/api/ready`
- Tip may lag HEAD — OK unless confirm present
- After deploy, start loop machine if suspended
- Do not set `ARIA_ALLOW_SKIP_LIVE_CALENDAR=1`
