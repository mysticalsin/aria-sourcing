---
project: MSourcing / ARIA
shift: 369
agent: cursor-cloud
updated: 2026-08-29T12:33Z
status: e2e-partial-awaiting-real-graph-secrets
---

# Handoff — Shift 369

## Current state

- **Branch / PR:** `cursor/enterprise-autopilot-b91d` · **PR #36** draft OPEN
- **Live Fly:** **`3fabbfa`** / **0074** · tip docs-ahead · loop primary **2863e10bd41e28**
- **PARTIAL E2E:** **58/0/2** — step 3c PASS · classifier=model
- **Graph:** `graph_secrets_missing=3` · **allowedToCreateApps=false** · zero fly.dev redirect apps · owned apps=0
- **Waiters:** noperm TTL **~5m** re-probe · Entra-admin messaging in az-create/probe/checklist
- **Gate:** audit **65/65**

## Done this shift

1. Re-scanned Entra: still no ARIA Fly app; BAW SAS present but must not use for Mantu Graph
2. Aligned az-create / probe / secrets-checklist with Entra-admin-must-register
3. Checklist copy-paste Teams/email block for Entra admin
4. Noperm TTL default 900→**300s** for faster role-grant detection

## Blockers

- Entra admin → register `ARIA Mantu Graph (Fly)` → `echo '<id>' > /tmp/owner-azure-app-id`  
  **or** Application Developer for `twalteur@amaris.com`  
  → Connect Outlook → `verify-m365-ready` → **RESULT: PASS**

## Next steps

```bash
bash scripts/print-m365-owner-portal-checklist.sh   # includes admin copy-paste
echo '<application-client-id>' > /tmp/owner-azure-app-id
bash scripts/probe-m365-unblock.sh --apply
# Settings → Connect Outlook → webhook + Calendars + OnlineMeetings
bash scripts/verify-m365-ready.sh   # RESULT: PASS
unset AGENT_PROVIDER AGENT_MODEL
bash scripts/run-enterprise-e2e-partial.sh
# expect step 3c PASS; RESULT: PARTIAL until live Graph seat
```

## Production gate (Fly)

```bash
bash scripts/print-fly-golive-status.sh
curl -fsS https://aria-mantu-app.fly.dev/api/ready | jq '{ok,build,migration}'
# step 3c should show PASS when running PARTIAL E2E
# Do NOT run verify-m365-ready until real Graph secrets + Connect Outlook.
```

## Decisions made (don't relitigate)

- Production = Fly only; PR #36 only; never BAW SAS for Mantu Graph
- allowedToCreateApps=false → Entra admin create or role grant
- Owner app-id dropzone + waiters; noperm TTL ~5m

## Watch out

- HANDOFF must keep “expect step 3c PASS” / “step 3c should show” + `print-fly-deploy-confirm`
- Never invent Microsoft secrets; reject synthetic app ids
- Do not remint tip_ahead_docs for Graph unblock
