---
project: MSourcing / ARIA
shift: 7
agent: codex
updated: 2026-07-10 00:04
status: candidate-agent-security-layer-tsc-clean
---

# Handoff - Candidate Agent Security Layer

## Current state
- Candidate-facing agent security layer is implemented in the working tree, uncommitted.
- `src/lib/agent-disclosure-policy.ts` now centralizes:
  - `DISCLOSABLE_JOB_FIELDS` default-deny whitelist for candidate role context.
  - `INJECTION_PATTERNS` and `detectInjection()` as triage signal.
  - salary inference blocklist and shared `validateCandidateBoundText()`.
  - `DISCLOSURE_SYSTEM` prompt fragment.
- Raw role/brief model inputs listed in `.rocket-fuel/design-agent-security.md` are routed through `candidateDisclosureContextForCampaignLike(...)`.
- Candidate-bound sinks now call the shared validator:
  - `src/lib/autopilot.ts`
  - `src/app/api/outreach/approve/route.ts`
  - `src/app/api/outreach/send/route.ts`
  - `src/app/api/outreach/whatsapp-review/route.ts`
  - `src/lib/dispatch-outbound.ts`
  - `src/lib/store.ts` reply-classification draft path
  - `src/app/api/sourcing-agent/route.ts` returned draft path
  - `src/lib/agents/graph.ts` generated draft path
- Careers route remains deterministic/no-LLM; a one-line comment marks target-salary capture as deferred schema/UI work.

## Done this shift
- Read `.rocket-fuel/design-agent-security.md`, `JobAnalysis`, all listed source/sink code, `_relay/HANDOFF.md`, vault rules, and Graphify skill.
- Graphify query and wiki lookup were attempted; this checkout has no `graphify-out/graph.json` or wiki index.
- Added `tests/agent-disclosure-policy.mts` with whitelist drop tests, 8+ injection fixtures, salary literal/inference blocks, allowed target-range ask, and drift guards.
- Replaced `roleSummary: JSON.stringify(brief)` and `JSON.stringify(state.brief)` prompt inputs.
- Appended `DISCLOSURE_SYSTEM` to the WhatsApp/autopilot reply system and delimited candidate inbound as untrusted data.
- Folded autopilot commitment behavior into the shared disclosure validator and re-exported `COMMITMENT_PATTERNS`.
- Wrote the local plan/review at `_agent_state/codex/plan-candidate-agent-security-layer.md`.

## Blockers
- `npx tsx tests/agent-disclosure-policy.mts` could not execute in this sandbox. It failed before test code with:
  - `Error: listen EPERM: operation not permitted ... /T/tsx-501/...pipe`
- This matches the prior sandbox limitation recorded in the previous baton. Visionary should run the focused test and guardrail suites outside the sandbox.

## Verification
- `npx tsc --noEmit` passed with exit 0.
- Static guard grep found no matches for:
  - `JSON.stringify(state.brief)`
  - `roleSummary:\s*JSON.stringify`
  - `JSON.stringify(brief)`
- `git diff --check` on touched files passed with exit 0.

## Next steps
1. Visionary runs `npx tsx tests/agent-disclosure-policy.mts` outside the sandbox.
2. Visionary runs the guardrail suites requested for this rock outside the sandbox.
3. Review dirty working tree carefully before commit; many files and package metadata were already dirty before this shift.
4. Commit this security layer separately from the prior Tavily/location/integration changes if possible.

## Decisions made (don't relitigate)
- The whitelist is the disclosure boundary; candidate-facing prompts receive only candidate-disclosable role facts.
- Salary-fit language is blocked generically even when exact salary context cannot be resolved.
- Injection detection is a triage flag, not the data boundary.
- Approved Meta WhatsApp template audit bodies remain exempt from free-form disclosure scanning because the stored body is not the candidate-visible template text.
- Do not weaken LinkedIn assisted-manual enforcement, outreach approval gates, SSRF/web-tool boundaries, or no-auto-send posture.

## Watch out
- `src/lib/store.ts`, `src/lib/dispatch-outbound.ts`, `src/app/api/sourcing-agent/route.ts`, and several other files had pre-existing unrelated changes in the working tree.
- `package.json` and `package-lock.json` are still dirty from prior work; this shift did not intentionally edit them.
- `_agent_state/codex/plan-candidate-agent-security-layer.md` is new and may be untracked.
