# Release Gate Matrix — MSourcing (Hermes Sourcing)

**Role:** Release Manager (final synthesis) · **Date:** 2026-06-27
**Inputs:** 13 review phases + foundation inventory + Red-Team review (`RED_TEAM_REVIEW.md`).
**Tree state:** git `main`, **WORKING TREE DIRTY** (~70 modified files, security migrations `0004`/`0005` + `supabase/config.toml` UNTRACKED, **no git remote**). All evidence is against the uncommitted tree.

Status legend: **PASS** (every required check verified, no open CRITICAL/HIGH) · **FAIL** (open CRITICAL/HIGH or a failing required check) · **UNKNOWN** (cannot verify / blocked on access). Per operating rules, UNKNOWN and FAIL are **not releasable**. Red-team overrides can only **downgrade** a domain verdict.

| Gate | Area | Status | Evidence | Open Findings | Severity | Owner | Fix/PR | Retest Result | Notes |
|---|---|---|---|---|---|---|---|---|---|
| 1 | Inventory / Foundation | **UNKNOWN** | App/repo inventory complete + evidence-backed (`INVENTORY.md`, `ASSET_REGISTER.md`: 14 handlers, 19 pages, 8 tables/5 migrations, RBAC, secret catalogue, 13 integrations). Prod-infra inventory blocked: `git remote -v` empty, no deploy, no cloud. | No off-machine source backup (no remote) → CI inert; prod infra not inventoriable | HIGH | Tony | — | n/a | Code inventory PASS-grade; infra UNKNOWN, blocked on access. Conservative rule → UNKNOWN |
| 2 | Threat model + risk register | **FAIL** | Threat model + register complete & current with file:line evidence (`THREAT_MODEL.md`, `RISK_REGISTER.md`). `npm audit` 2026-06-27 = 4 high + 1 moderate (incl. SSRF GHSA-c4j6-fc7j-m34r). Security test subset 87 assertions pass. | Vulnerable Next.js; plaintext secrets at rest; tenant isolation runtime-unverified; demo auth bypass; PII in logs; no rate limiting | HIGH | Tony+Eng | partial | n/a | Deliverable complete; production-readiness verdict FAIL on open HIGHs |
| 3 | Frontend / UX / Accessibility / Frontend-Sec / Frontend-Perf | **FAIL** | typecheck exit 0, `next lint` clean. UX strong (skeletons, empty states, accessible confirm modal, safe defaults). | No error boundary (app-wide white-screen, HIGH); CSP unsafe-inline/eval (HIGH); next@14.2.35 CVEs (HIGH); WCAG AA defects (status announce, contrast, autoplay video Level A); non-HttpOnly tokens + localStorage PII; recharts/landing bundle; unbounded global context re-render; CWV unmeasured; **no axe/SR/keyboard pass run** | HIGH | Frontend | partial | typecheck/lint PASS; a11y scan not run (UNKNOWN) | 5 sub-domains; all FAIL or carry HIGH; a11y manual pass + CWV are UNKNOWN |
| 4 | Backend / API / Authz / Business-logic | **FAIL** | Auth-first handlers, zod validation, SSRF allow-list, per-task RBAC, atomic `claim_and_record`; 705/705 tests + typecheck pass (run sandbox-bypassed). | **Fail-open demo mode (CRITICAL, red-team)**; Hermes proxy unauth relay + BFLA GET reads in demo (HIGH); no rate limiting (HIGH); workspace_state member-writable + last-write-wins (HIGH); suppression/DNC never reaches server guardrail (HIGH); next@14.2.35 (HIGH); CRLF MIME injection, stale claims, body-buffer DoS, OAuth CSRF, plaintext tokens (MED) | CRITICAL | Eng | partial | unit tests PASS; 0/15 handlers exercised; negative authz tests absent | Red team: BOLA/RBAC/admin-only PASSes downgraded to UNKNOWN/FAIL (demo fail-open + design-only) |
| 5 | Data / Database / Data-protection | **FAIL** | 8-table workspace-scoped RLS model present & well-designed (`0001`–`0005`); column-grant secret withholding; render-masking tested. | Security migrations `0004`/`0005` + `config.toml` UNTRACKED (not in deployable artifact); workspace_state UPDATE has **no role predicate** (any viewer overwrites tenant state — red team downgrades RLS-design PASS→FAIL); outreach_ledger member-mutable (audit-tamper); plaintext secrets/tokens, no KMS; RLS has **zero automated verification**; retention unenforced; erasure/export/account-removal incomplete; PII in logs; backups/PITR + prod-auth UNKNOWN | HIGH | Eng+Tony | — | RLS untested (no DB access); in-transit TLS UNKNOWN | Red team: DB-roles-least-priv PASS→UNKNOWN (lives only in untracked 0004/0005) |
| 6 | Infrastructure / Network / IAM / TLS | **FAIL** | App primitives strong: SSRF guard, RLS+column grants, RBAC, least-priv OAuth scopes (gmail.send / Mail.Send). | next@14.2.35 unpatched HIGH advisories + non-blocking audit (HIGH); plaintext secrets at rest (HIGH); demo-mode fail-open (HIGH); CSP/header drift + unsafe-inline/eval (MED); no rate-limit/WAF (MED); Hermes allow-list private-IP-only = unreachable from Vercel (MED). Live TLS/IAM/exposure UNKNOWN | HIGH | Tony+Eng | — | Live TLS/IAM scans blocked on access | Large UNKNOWN live plane; remediate 3 HIGHs then re-scan |
| 7 | Containers / Orchestration | **UNKNOWN** | Current deploy containerless (Vercel serverless, `vercel.json`). No Dockerfile/compose/k8s/helm/terraform at root. | Vendored `ultraplan/claw3d/Dockerfile` (locked forward plan) fails hardening: root user, unpinned base, no HEALTHCHECK, no scan/SBOM/CI build; no root `.dockerignore` (merge would bake `.env.local`); never built/scanned | HIGH | Eng | — | image never built/scanned | N/A-now for live deploy; conservatively UNKNOWN given locked Docker forward plan |
| 8 | CI/CD + Supply chain | **FAIL** | `ci.yml`+`codeql.yml` present; `npm ci` + lockfile v3; tests green locally; no secrets in CI. | **No git remote → CI/CodeQL/gitleaks never ran** (HIGH); `npm audit ... \|\| true` non-blocking with 4 high (HIGH); no prod-deploy approval/Environment (HIGH); rollback/PITR never drilled (HIGH/UNKNOWN); no CODEOWNERS/Dependabot/SBOM/provenance; actions on mutable tags; Node drift (CI 20 vs local 22) | HIGH | Tony+Eng | — | pipeline has zero run history | Every green local gate is unbound to a deployable, CI-verified artifact (red-team meta-override) |
| 9 | QA / Testing | **FAIL** | 4 scripted gates green: 705/705 assertions (22 suites), typecheck 0, lint clean, build 0 (36 routes). | **0/15 server entry points** (8 API + 6 auth + middleware) executed by any test; tenant isolation + OAuth tokens untested (HIGH); coverage **uninstrumented (UNKNOWN)**; no E2E/a11y/load tier; static-regex "security tests" with `\|\|` escape hatch | HIGH | QA+Eng | — | tests PASS but coverage of sensitive paths = 0 | Untested sensitive paths = FAIL per "untested!=PASS" rule |
| 10 | Performance / Reliability / Capacity | **FAIL** | Hermes/LLM 30s timeout + mock fallback; atomic de-dupe claim. | Unbounded full-state JSONB persisted ~per mutation (HIGH); last-write-wins data loss (HIGH); no timeout on email/OAuth/DNS (HIGH); next@14.2.35 DoS advisory (HIGH); no retries/circuit-breakers; no maxDuration; stuck claims; save-errors swallowed; single region no failover. **No SLOs / no load test (UNKNOWN)** | HIGH | Eng | — | load/SLO unmeasured, blocked on staging | Capacity entirely unmeasured |
| 11 | Observability / Operations | **FAIL** | `/api/health` liveness exists. | No error-tracking/metrics/traces/alerting/synthetic checks (HIGH); candidate PII + provider bodies in logs (HIGH); no durable tamper-evident audit log (HIGH); no security-signal alerting (HIGH); log retention UNKNOWN; on-call = placeholders | HIGH | Tony+Eng | — | operationally blind | No incident detection path; PII-free-logs and sensitive-action-audit checks FAIL |
| 12 | Backup / Restore / DR | **FAIL** | Runbooks exist (`ROLLBACK_RUNBOOK.md`, `DISASTER_RECOVERY_PLAN.md`). | Prod backups/PITR assumed not proven (**CRITICAL/UNKNOWN**; PITR off by default); **restore never drilled** — couldn't run (Docker absent) (**CRITICAL/OPEN**); backup artifacts dump cleartext secrets+PII and `backups/` not gitignored (HIGH); RTO/RPO undefined + last-write-wins silent loss (HIGH); no off-site source (no remote) (HIGH); runbook schema drift; destructive rollback untested | CRITICAL | Tony+Eng | — | restore unprovable (no infra, no Docker) | Hard blocker before any real PII |
| 13 | Privacy / Compliance / Governance | **FAIL** | App RBAC/RLS strong in source; render-masking tested. | Compliance target undefined; no privacy notice/ROPA/DPIA (HIGH); decorative compliance toggles — zero consumers (HIGH); candidate PII + reply text to cloud LLMs, no DPA/no-train (HIGH); algorithmic ranking = profiling + likely EU AI Act high-risk, unassessed (HIGH); broken outreach opt-out (placeholder `hermes.example`) (HIGH); no subprocessor register/DPAs (HIGH); over-broad internal PII access (HIGH); service-role unmonitored superuser (HIGH); audit not tamper-evident; residency/DPAs/provider-plane UNKNOWN | HIGH | Tony/DPO+legal | — | regimes undeclared; breach 72h detection unproven | Needs human/DPO decision before any PASS is possible |
| 14 | Final release sign-off (adversarial) | **FAIL** | Synthesis of all gates + `RED_TEAM_REVIEW.md`. | 1 CRITICAL open (fail-open) + 1 CRITICAL data-loss + ~30 HIGH open/unknown across 12 gates; multiple automatic blockers tripped | CRITICAL | Release Mgr | — | — | Cannot sign off while Gates 2–13 are FAIL/UNKNOWN |

---

## Tally

**PASS: 0/14 · FAIL: 12 · UNKNOWN: 2 (Gates 1, 7) · PARTIAL: 0.**

## Red-team overrides applied (downgrades only)
- **[CRITICAL]** Cross-cutting: fail-open demo mode shippable to prod with no hard guard → drives Gate 4 to CRITICAL and taints every auth/RBAC/admin PASS.
- **[HIGH]** Gate 4 authz: cross-workspace BOLA PASS → **UNKNOWN** (design-only, untracked migration, zero negative tests).
- **[HIGH]** Gate 5: RLS-design PASS → **FAIL** (workspace_state UPDATE no role predicate); DB-roles-least-priv PASS → **UNKNOWN** (lives only in untracked 0004/0005).
- **[HIGH]** Gate 4 API-sec: Hermes-proxy SSRF allow-list PASS → **UNKNOWN** (redirect-follow not blocked; unauth in demo; private-IP-only allow-list unreachable from Vercel).
- **[HIGH]** Gate 4 authz: admin-only mutating routes PASS → **FAIL** (requireAdmin fails open in demo; `/api/keys/test` value-path returns before auth).
- **[HIGH]** Gate 4 API-sec: per-endpoint RBAC PASS → **UNKNOWN** (pure `can()` table only; 0/15 handlers executed).
- **[HIGH]** Cross-cutting: anon-key-designed-public / secrets-withheld PASS contingent on **unverified, untracked** RLS.
- **[HIGH]** Meta: every green gate (typecheck/test/lint/build + source PASSes) is **unbound to a deployable artifact** (dirty tree, untracked security migrations, no remote, CI never executed).
- **[MEDIUM]** Gate 4: versioning/idempotency PASS → **FAIL** (idempotency lives in unwired `/api/outreach/send`; wired `approveOutreach` has no idempotency guard).
- **[MEDIUM]** Gate 5: encryption-in-transit PASS → **UNKNOWN** (`config.toml` ssl_enforcement commented out; sslmode unpinned; prod TLS unverified).
- **[MEDIUM]** Cross-cutting: test-based security PASSes overstate assurance (static-regex with self-defeating `||` escape hatch; never run in CI).
- **[LOW]** Gate 5: password-storage PASS → **UNKNOWN** (self-described; hardcoded `admindemo123` fallback; `minimum_password_length=1`).

## Final verdict: **NOT READY**
0/14 gates PASS. Two CRITICAL items open (fail-open auth-bypass deploy path; unproven backups + never-drilled restore) plus ~30 open/unknown HIGH findings, and every locally-green check is unbound to a committed, CI-verified release artifact. See `PRODUCTION_READINESS_REPORT.md`.
