risk: `src/lib/store.ts:793-812` is a candidate-bound live prompt source for follow-up and recontact drafts via `attemptLiveFollowUpGen()`, used at `src/lib/store.ts:2214-2225` and `2272-2283`. It currently passes only reduced role fields, but v2’s exact source list names `2095-2111` and `2336-2350`, not this helper. Treat every `buildOutreachPrompt()` call as covered by `toCandidatePublicRoleContext()`, not just the two listed ranges.

risk: sink coverage is directionally complete, but the build needs a clear internal-context resolver per sink. `/api/outreach/approve` only receives body/scope fields today, `/api/outreach/send` receives `campaignId`, WhatsApp review has `messageId`, and dispatch has an outbox row. The design is sound if each sink can fetch salary/forbidden context or uses a strict generic comp block when context is unavailable.

risk: salary allowlist should add adjacent inference phrasing beyond the examples: “too high”, “out of budget”, “we can meet that”, “we cannot meet that”, “aligned with expectations”, “not aligned”, “stretch”, “below expectations”, “hard to justify”, “market-aligned”, and “close enough”. The safe outputs should remain only asking for the candidate’s target range or deferring compensation to a recruiter.

question: `src/app/api/hermes/chat/route.ts:239-280` can bind a full `campaign` to the sourcing tool loop, and `src/lib/ai/tool-loop.ts:171,294` feeds tool outputs back to the model. I do not count this as candidate-facing because it returns to internal chat, not a candidate sink. If Claude intends chat output to be reusable as candidate copy, it needs the same sink gate.

nit: make the proof grep for every `buildOutreachPrompt(`, `JSON.stringify(state.brief)`, `roleSummary`, `gateOutbound(`, `recordOutreachApproval`, `review_whatsapp_outbound`, and `dispatchDue` call. That catches future drift better than hardcoded line references.

nit: expose one helper like `candidateDisclosureContextForCampaignLike()` so callers do not hand-map `JobAnalysis` fields differently across WhatsApp, graph, sourcing-agent, and store flows. That keeps the owner’s “easy to modify” requirement intact.

VERDICT: APPROVED