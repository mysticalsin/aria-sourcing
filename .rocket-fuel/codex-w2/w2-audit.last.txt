wave2: WINLOG — add a durable `win_log`/`win_patterns` store entry at the successful booking commit in `createBookingFor`, not a markdown append; that hook has candidate, campaign, booking, reply history, outreach history, metrics, booking activity, and `emit({ kind: "book" })` in one place (`src/lib/store.ts:3407`, `src/lib/store.ts:3471`, `src/lib/store.ts:3482`, `src/lib/store.ts:3498`); do not reuse current weekly-report winning patterns as truth because they are fixed illustrative strings (`src/lib/mock-ai.ts:1440`, `src/components/reports/weekly-report-card.tsx:133`).
wave2: EXEC DASHBOARD — feasible as a read-only `/exec` top-management view over real `HermesState` slices: `campaigns`, `candidates`, `outreach`, `replies`, `bookings`, `activities`, `ledger`; reuse `globalKpis` and extend it with per-campaign/per-platform funnels from candidate `sourcePlatform`, outreach `channel`, ledger `status`, and booking counts (`src/lib/types.ts:1253`, `src/lib/metrics.ts:140`, `src/lib/metrics.ts:155`, `src/app/page.tsx:65`, `src/components/floor/mission-control-hud.tsx:16`, `src/lib/replay.ts:6`).
wave2: EXEC DASHBOARD — missing metric: open rate is not available for email/LinkedIn because `CampaignMetrics` has no opens fields and only WhatsApp has provider receipt/read events; label open rate unavailable until an email-open/click event table or provider webhook is added (`src/lib/types.ts:680`, `supabase/migrations/0010_whatsapp_delivery_reconciliation.sql:11`, `src/app/api/webhooks/whatsapp/route.ts:67`).
wave2: DATABRICKS — cleanest seam is a first-party `/api/integrations/databricks` route using SQL Warehouse statement execution, resolving the token through the encrypted `api_keys` vault, mapping returned rows into the same intake shape consumed by `parseIntakeLive`/`createCampaignFromAnalysis`; current key vault and MCP routes are ready patterns, but `Databricks` is not yet a first-class provider (`src/lib/ai/vault-secret.ts:5`, `src/app/api/keys/route.ts:17`, `src/lib/types.ts:1019`, `src/app/api/mcp/test/route.ts:14`, `src/app/intake/page.tsx:239`, `src/app/intake/page.tsx:328`).
refine: email inbox replies are not durably ingested server-side like WhatsApp; `/api/email/sync` returns message bodies to the browser, then `RepliesPage` loops through `classifyAndStoreReply`, then workspace JSON saves on a debounce, so a tab/client failure can drop positive replies before persistence (`src/app/api/email/sync/route.ts:197`, `src/app/replies/page.tsx:83`, `src/lib/store.ts:947`).
refine: production KPI semantics are inconsistent: campaign metrics count `contacted` from candidate stage high-water rank, while the mission HUD counts contacted from ledger rows with `status === "sent"`; exec reporting needs one canonical definition from the ledger/outreach send facts (`src/lib/metrics.ts:103`, `src/lib/metrics.ts:129`, `src/components/floor/mission-control-hud.tsx:18`, `src/components/floor/mission-control-hud.tsx:80`).
refine: live sourcing-agent APIs still accept full client-owned `campaign` and `existing` objects instead of loading the stored campaign/spec server-side, so an API caller can source/draft against modified role data that is not the shared workspace record (`src/app/api/sourcing-agent/route.ts:36`, `src/app/api/sourcing-agent/route.ts:123`, `src/app/api/agents/run/route.ts:33`, `src/app/api/agents/run/route.ts:72`).
refine: LinkedIn manual-send confirmation writes only a local in-app ledger entry with `seatId: ""`; it does not record a server `outreach_ledger` row, so cross-tab/shared-workspace de-dupe can drift from the database enforcement path (`src/lib/store.ts:2647`, `src/lib/store.ts:2678`, `supabase/migrations/0002_fleet.sql:42`, `supabase/migrations/0002_fleet.sql:55`).
refine: current learning loop can propose/action skill updates from coarse stage outcomes, but it lacks a structured win record with touches-to-yes and message traits; `learnedParamsFor` has only aggregate tone/dimension signals, so Wave-2 winlog should feed this layer rather than another illustrative report list (`src/lib/skills.ts:182`, `src/lib/skills.ts:223`, `src/lib/skills.ts:251`).
VERDICT: APPROVED