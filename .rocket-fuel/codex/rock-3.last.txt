Implemented Rock 3.

Changed `src/lib/integrations.ts` so all `real:false` cards seed as `not_configured` with `lastSync:null`, and `testConnection()` now fails closed for placeholders. Added `STATE_VERSION = 16` migration so older persisted placeholder cards are normalized too. Updated `dispatch-outbound.ts` with `stats.unconfigured` so dry-run provider outcomes no longer increment `stats.failed`.

Added `tests/integrations-honesty.mts`.

Verification:
`node --import tsx tests/integrations-honesty.mts` -> `RESULT integrations-honesty: 6 passed, 0 failed`
`node --import tsx tests/dispatch-outbound.mts` -> `RESULT dispatch-outbound: 79 passed, 0 failed`
`npx tsc --noEmit` -> exit 0
`npm test` exact command is still sandbox-blocked by `tsx` IPC `EPERM`; full suite equivalent with bare `tsx` rewritten to `node --import tsx` passed.


ROCK-STATUS: DONE