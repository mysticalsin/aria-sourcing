Built the Tavily MCP query-auth integration and kept bearer behavior as the default. Key paths now use `applyMcpAuth`, base URLs reject `tavilyApiKey`, route schemas enforce the enum, settings exposes the admin controls, the probe uses the same assembly path, and MCP errors are host-only.

Proof:
`node --import tsx tests/mcp-query-auth.mts` → 15 passed, 0 failed
`node --import tsx tests/log-redact.mts` → 24 passed, 0 failed
`npx tsc --noEmit` → clean
`git diff --check` → clean

Live Tavily probe was not run here; the brief assigns that to Visionary outside the sandbox.

VERDICT: SHIP

