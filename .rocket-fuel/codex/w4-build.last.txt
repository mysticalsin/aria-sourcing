Built Rock W4.

Added Databricks config/provider support, key-id secret resolution, Supabase-free Statement Execution client, hardened `/api/integrations/databricks/needs` route, admin settings UI, and `tests/databricks-intake.mts`.

Proof:
`node --import tsx tests/databricks-intake.mts` → `RESULT databricks-intake: 15 passed, 0 failed`
`npx tsc --noEmit` → exit 0
`git diff --check` on W4 files → exit 0

SHIP = built + tsc clean + test encodes (a)-(f).

VERDICT: SHIP