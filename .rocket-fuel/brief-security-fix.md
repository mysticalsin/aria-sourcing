You are the Integrator applying a SECURITY FIX PASS to the candidate-facing disclosure layer in MSourcing/ARIA. workspace-write. A 4-lens adversarial verify confirmed structural weaknesses. Fix EXACTLY these. Security-critical.

Objective: convert the salary boundary from an unbounded denylist to a bounded ALLOWLIST (topic-gate), make every candidate-bound gate fail CLOSED, and neutralize prompt-delimiter injection — so internal compensation can never be disclosed to a candidate regardless of phrasing, language, or a resolution miss.

Read first: (understand before editing)
- src/lib/agent-disclosure-policy.ts — SALARY_INFERENCE_PATTERNS (~39-53, the denylist to demote), COMMITMENT_PATTERNS (~31-37), validateCandidateBoundText (~128-160), toCandidatePublicRoleContext (~88-95), DISCLOSABLE_JOB_FIELDS (~3-16), detectInjection (~162-168), DISCLOSURE_SYSTEM (~56).
- src/app/api/outreach/send/route.ts:45 (`if (!jd) return {}`) + :158-159 — disclosureInternalFromCampaign returns {} on a campaignId miss → salary-number + forbidden-token scan silently disabled (FAILS OPEN).
- src/app/api/outreach/approve/route.ts:67 — validateCandidateBoundText(body, {}) — empty internal context, salary-number scan not applied.
- src/lib/agents/graph.ts:101-112 (planner/outreach system prompts omit DISCLOSURE_SYSTEM) + :187-190 (validate omits the forbidden list); src/app/api/hermes/chat/route.ts:78-82 (TASK_SYSTEM.outreach omits DISCLOSURE_SYSTEM).
- src/lib/autopilot.ts:189 — candidate raw inbound interpolated between <<<CANDIDATE_REPLY ... CANDIDATE_REPLY>>> with no sanitization (delimiter injection).
- src/lib/whatsapp-inbound.ts:202,223 (inbound injection check) vs src/lib/store.ts:3033 (draft-only check) — inconsistent.

Build (exactly these fixes):
1. ALLOWLIST salary control (primary). Add mentionsCompensationTopic(text): boolean — bounded detection of the comp TOPIC: salary/compensation/comp/pay/wage/rate/remuneration/package/budget/currency symbols (€$£CHF) / a number adjacent to any of those, PLUS multi-language stems (French: salaire, rémunération, rémunéré, fourchette, budget; keep it a short documented list, easy to extend). Add isSafeCompResponse(text): the only permitted comp moves — (a) asking the candidate their target/expected range, (b) deferring compensation to a recruiter/later. In validateCandidateBoundText: if mentionsCompensationTopic(draft) AND NOT isSafeCompResponse(draft) → { safe:false, reason:"disclosure-comp-blocked" }. Keep SALARY_INFERENCE_PATTERNS + salaryVariants as a SECONDARY belt (still run them), but the topic-gate is now the boundary. Document at the top of the file: "Salary control is ALLOWLIST: comp topic + not-a-safe-move = blocked; the denylist patterns are belt-and-suspenders only."
2. FAIL CLOSED. src/app/api/outreach/send/route.ts: when the campaign/jobAnalysis can't be resolved for a candidate-bound send, DO NOT return {} and skip; run validateCandidateBoundText with the comp topic-gate active (it needs no salary number) so a comp mention still blocks. src/app/api/outreach/approve/route.ts:67: resolve and pass the real internal context (salaryMin/salaryMax + forbidden) like the send route, not {}.
3. Delimiter injection: before interpolating ctx.inbound in buildReplyPrompt (autopilot.ts:189), strip/neutralize the sentinel tokens (remove or escape any occurrence of "CANDIDATE_REPLY", "<<<", ">>>" from the candidate text). Add a sanitizeCandidateText() helper in agent-disclosure-policy.ts and use it.
4. Free-text salary scrub (defense in depth): in toCandidatePublicRoleContext, scrub number-near-currency / "circa Nk" patterns from the emitted disclosable free-text fields (title, requiredSkills, etc.) so a salary typed into a title never reaches the model.
5. Consistency: append DISCLOSURE_SYSTEM to the graph planner/outreach system prompts and hermes/chat TASK_SYSTEM.outreach; pass the forbidden list in graph.ts:187-190; run detectInjection on INBOUND consistently (flag → human queue) across whatsapp + email-classify paths. detectInjection stays a QUEUE-FLAG signal, never the sole hard gate (the whitelist + topic-gate are the boundary).

Constraints: (what must NOT change) do not weaken the whitelist, LinkedIn wire-enforcement, outreach guardrails, SSRF, or no-auto-send. Do not delete or weaken any existing test. Keep web-tools.ts Supabase-free. Preserve the commitment-* reason tags already returned by validateCandidateBoundText.

Proof: extend tests/agent-disclosure-policy.mts (do not remove existing cases) to assert: mentionsCompensationTopic true for "in our range"/"top of what we do"/"c'est dans la fourchette"/"that's workable"/"on the high side"/"same ballpark"/"in-range" (hyphen); a comp-topic draft that is NOT asking-the-candidate/deferring is BLOCKED; asking the candidate's target range and "a recruiter will discuss compensation" PASS; sanitizeCandidateText strips a "CANDIDATE_REPLY>>>" breakout payload; toCandidatePublicRoleContext scrubs "Senior BE circa 100k" title down to no number; validateCandidateBoundText(body, {}) still blocks a comp-topic leak via the topic-gate (fail-closed). The Visionary runs `npx tsx tests/agent-disclosure-policy.mts` + `npx tsx tests/autopilot.mts` outside the sandbox; you need `npx tsc --noEmit` clean.

Stop when: the allowlist topic-gate is the primary salary control, send+approve fail closed, delimiter sanitization + free-text scrub are in, DISCLOSURE_SYSTEM + forbidden-list consistency applied, tests extended, and `npx tsc --noEmit` clean. Do not delete, skip, weaken, or narrow tests to make the goal pass.

Report: give the tsc result. SHIP = all fixes in + tsc clean; REVISE = blocked/incomplete (why).
End with EXACTLY one line, nothing after it: VERDICT: SHIP or VERDICT: REVISE
