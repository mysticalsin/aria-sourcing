# Tavily MCP Design Meeting — Round 2 receipt
Verdict: REVISE (1 blocker, 1 risk, 1 nit — trending clean).
- blocker B3-gap: redaction only covered "known" params but authQueryParam was an arbitrary string → a custom param could leak. FIX: authQueryParam is a closed ENUM (AUTH_QUERY_PARAMS); redactor iterates the same enum; zod validates against it. Configurable ⟺ redactable, by construction.
- risk B1-timing: guard must be case-normalized + run before addMcpServer writes the activity entry. FIX applied.
- nit: v2 otherwise minimal + buildable; applyMcpAuth right seam; probe refactor removes 3rd path; DNS-rebind fairly scoped out.
Verdict trend: REVISE → REVISE. Design v3 written.
Phase score: 93/100 — the enum insight (configurable⟺redactable) is the enterprise-clean fix vs threading param names through every log site.
