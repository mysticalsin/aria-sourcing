# Tavily MCP Design Meeting — Round 1 receipt
Method: co-founder (V: claude · I: codex gpt-5.5) · fresh read-only thread · Verdict: REVISE
Owner standard bound: enterprise-clean, senior-dev-readable, no slop, secret-safe.

3 blockers + 2 risks + 1 question + 1 nit — ALL ACCEPTED (none rebutted):
- B1: admin could paste ?tavilyApiKey= into the base url → persisted + shown in UI + activity note (mcp-servers-panel:48, store.ts:5472). FIX: reject/scrub auth query params from base url at save (store + both route schemas + UI) + applyMcpAuth throws on it.
- B2: new auth fields never reach runtime unless store payloads (store.ts:5530,:5954) + both route zod schemas (hermes/chat:63, mcp/test:20) accept+forward them. FIX: thread end-to-end.
- B3: error surfaces leak the query secret — mcp-client returns raw error.message (:90,:131,:144), test route returns err.message (:91,:93), chat logs tool-loop reasons (:287); redactSecrets doesn't catch tavilyApiKey=. FIX: centralized query-secret redaction + host-only error strings.
- risk: DNS rebind on tool-call redial (hermes/chat:143, tool-loop:55) — pre-existing, all MCP servers → OUT OF SCOPE, documented + logged TODO.
- risk: ResolvedMcpServer key-bearing url is a fragile secret container → covered by centralized redaction.
- Q: probe script is a 3rd dialer hand-assembling the url → refactor to applyMcpAuth (one assembly path).
- nit: applyMcpAuth reject existing authQueryParam in base.

Design v2 written. Verdict trend: REVISE.
Phase score: 90/100 — my v1 said "never logs" without proving the redaction/validation/schema-threading that makes it true; Codex found all 3 leak paths I hand-waved. Enterprise-clean means the secret-safety is ENFORCED, not asserted. (IMPROVE.md.)
