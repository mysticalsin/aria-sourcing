# Deployment

Canonical production runbook: [`production-readiness/DEPLOYMENT_RUNBOOK.md`](production-readiness/DEPLOYMENT_RUNBOOK.md).

Vercel demo branch variant: use the same runbook, but deploy the `vercel-demo` branch and keep demo-only credentials separate from production.

Required env examples: [`.env.production.example`](.env.production.example) and [`.env.local.example`](.env.local.example).

Current release posture: [`production-readiness/STATUS.md`](production-readiness/STATUS.md).
